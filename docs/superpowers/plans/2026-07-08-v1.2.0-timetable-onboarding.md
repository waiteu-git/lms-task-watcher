# v1.2.0 時間割UI・オンボーディング Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v1.2.0リリースに向け、時間割UIの3改善（ポップアップに今日の時間割・課題の緊急度カラーバッジ・コース更新NEWバッジ）と、コース自動選択＋ウェルカムガイド改訂を実装する。

**Architecture:** 純粋ロジック（締切tier判定・自動選択・緊急度集計）を `src/core`・`src/utils` に切り出しVitestで単体テスト、UIは既存の `TimetableSection` に加え `TodayTimetable` を新設。コース自動選択はbackground（`UPSERT_COURSES`ハンドラ＋`chrome.storage.onChanged`）で発火する。

**Tech Stack:** React 19 + TypeScript + Vite、node-html-parser、Vitest、chrome.storage.local。

## Global Constraints

- background/content/popupはすべてTypeScript。`pnpm build`（`tsc -b && vite build`）でバンドル。**`dist/` を直接編集しない**。
- `src/content/classTimetable.ts` には**import文を追加しない**（classic content scriptがRollup共有チャンク化で壊れるため。現状 `chrome.storage.local.set` 直書き）。
- `Course` 型への追加フィールドは**optional（後方互換）**にする。既存の保存データが `undefined` で従来通り動くこと。
- テスト実行: `pnpm vitest run <path>`。全体品質ゲート: `pnpm vitest run src` / `pnpm lint` / `pnpm build`。
- changelog（`public/changelog.html`）更新時は**リタス（Litus）事前登録導線** https://lms.waiteu.dev/app を必ず掲載（CLAUDE.mdルール）。
- コミットメッセージ末尾に `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。作業ブランチは `feature/v1.2.0-timetable-onboarding`（作成済み）。
- 緊急度2段階: **当日中（カレンダー上の今日）=赤 / 今日を除く7日以内=橙 / それ以外=無印**。判定対象は未提出・期限切れ除外・開始前除外の課題＋未提出の手動課題（締切あり）。突合キーはLETUSコース名の埋込7桁コード（`extractCourseCodes`）。
- コース自動選択は**時間割にいずれか1コードが一致すれば自動ON**。`userToggled` が立ったコースは自動選択で触らない。**自動DISABLEはしない**（自動ENABLEのみ）。

---

### Task 1: 締切tier判定 `deadlineTier`（純粋関数）

**Files:**
- Modify: `src/utils/date.ts`
- Test: `src/utils/date.test.ts`（新規。既存の date テストは無い）

**Interfaces:**
- Produces: `export type DeadlineTier = 'today' | 'week' | 'none'` / `export function deadlineTier(deadline: string | null, now: Date): DeadlineTier`
- Consumes: `SEVEN_DAYS_MS`（`../constants`、既存import）

- [ ] **Step 1: 失敗するテストを書く**

`src/utils/date.test.ts` を新規作成:

```ts
import { describe, it, expect } from 'vitest'
import { deadlineTier } from './date'

const now = new Date('2026-07-08T10:00:00+09:00')

describe('deadlineTier', () => {
  it('当日中の締切は today', () => {
    expect(deadlineTier('2026-07-08T23:59:00+09:00', now)).toBe('today')
    expect(deadlineTier('2026-07-08T10:30:00+09:00', now)).toBe('today')
  })
  it('翌日〜7日以内は week', () => {
    expect(deadlineTier('2026-07-09T09:00:00+09:00', now)).toBe('week')
    expect(deadlineTier('2026-07-15T09:00:00+09:00', now)).toBe('week')
  })
  it('7日より先は none', () => {
    expect(deadlineTier('2026-07-16T09:00:00+09:00', now)).toBe('none')
  })
  it('過去・null・不正は none', () => {
    expect(deadlineTier('2026-07-07T09:00:00+09:00', now)).toBe('none')
    expect(deadlineTier(null, now)).toBe('none')
    expect(deadlineTier('not-a-date', now)).toBe('none')
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm vitest run src/utils/date.test.ts`
Expected: FAIL（`deadlineTier` is not exported）

- [ ] **Step 3: 実装を追加**

`src/utils/date.ts` の末尾に追加:

```ts
export type DeadlineTier = 'today' | 'week' | 'none'

/** 締切の緊急度tier。当日中(カレンダー上の今日)=today / 今日を除く7日以内=week / それ以外・過去・不正=none。 */
export function deadlineTier(deadline: string | null, now: Date): DeadlineTier {
  if (!deadline) return 'none'
  const d = new Date(deadline)
  const t = d.getTime()
  if (Number.isNaN(t)) return 'none'
  const nowT = now.getTime()
  if (t < nowT) return 'none'
  if (
    d.getFullYear() === now.getFullYear() &&
    d.getMonth() === now.getMonth() &&
    d.getDate() === now.getDate()
  ) {
    return 'today'
  }
  return t - nowT <= SEVEN_DAYS_MS ? 'week' : 'none'
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `pnpm vitest run src/utils/date.test.ts`
Expected: PASS（4 tests）

- [ ] **Step 5: コミット**

```bash
git add src/utils/date.ts src/utils/date.test.ts
git commit -m "feat(core): add deadlineTier urgency helper

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: 時間割の科目コード集合 `getCapturedCourseCodes`

**Files:**
- Modify: `src/core/timetableView.ts`
- Test: `src/core/timetableView.test.ts`（新規）

**Interfaces:**
- Produces: `export async function getCapturedCourseCodes(year: number, semester: Semester): Promise<string[]>`（時間割キャプチャをパースして重複なしの7桁コード配列を返す。未取得は `[]`）
- Consumes: `getTimetableCapture`（`./timetableStore`）、`parseTimetable`（`./timetable`）、`TABLE_MINIMAL`（`./timetable.fixtures`、テスト用・コード `9973337` を含む）

- [ ] **Step 1: 失敗するテストを書く**

`src/core/timetableView.test.ts` を新規作成:

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { TABLE_MINIMAL } from './timetable.fixtures'

const store: Record<string, unknown> = {}
vi.stubGlobal('chrome', {
  storage: {
    local: {
      get: vi.fn(async (keys: string | string[]) => {
        const result: Record<string, unknown> = {}
        for (const k of Array.isArray(keys) ? keys : [keys]) result[k] = store[k]
        return result
      }),
      set: vi.fn(async (obj: Record<string, unknown>) => { Object.assign(store, obj) }),
    },
  },
})

const { getCapturedCourseCodes } = await import('./timetableView')

beforeEach(() => { for (const k of Object.keys(store)) delete store[k] })

describe('getCapturedCourseCodes', () => {
  it('未取得なら空配列', async () => {
    expect(await getCapturedCourseCodes(2026, 'zenki')).toEqual([])
  })
  it('キャプチャHTMLから7桁コードを抽出する', async () => {
    store['timetable:2026:zenki'] = { rawTableHtml: TABLE_MINIMAL, jigenText: '', capturedAt: '2026-07-08T00:00:00.000Z' }
    const codes = await getCapturedCourseCodes(2026, 'zenki')
    expect(codes).toContain('9973337')
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm vitest run src/core/timetableView.test.ts`
Expected: FAIL（`getCapturedCourseCodes` is not exported）

- [ ] **Step 3: 実装を追加**

`src/core/timetableView.ts` に追加（既存importに `getTimetableCapture` と `parseTimetable` を足す）:

```ts
import { parseTimetable } from './timetable'
// 既存の import { getPreferredView, listCapturedSemesters, getOverrides } に getTimetableCapture を追加
import { getPreferredView, listCapturedSemesters, getOverrides, getTimetableCapture } from './timetableStore'

/** 取得済み時間割から重複なしの7桁科目コード配列を返す（未取得は空）。 */
export async function getCapturedCourseCodes(year: number, semester: Semester): Promise<string[]> {
  const cap = await getTimetableCapture(year, semester)
  if (!cap) return []
  const codes = new Set<string>()
  for (const s of parseTimetable(cap.rawTableHtml)) {
    for (const c of s.classes) if (c.courseCode) codes.add(c.courseCode)
  }
  return Array.from(codes)
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `pnpm vitest run src/core/timetableView.test.ts`
Expected: PASS（2 tests）

- [ ] **Step 5: コミット**

```bash
git add src/core/timetableView.ts src/core/timetableView.test.ts
git commit -m "feat(core): add getCapturedCourseCodes

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: コース自動選択の純粋関数＋Course型拡張

**Files:**
- Modify: `src/core/types.ts`（`Course` に `userToggled?: boolean` 追加）
- Create: `src/core/courseSelect.ts`
- Test: `src/core/courseSelect.test.ts`

**Interfaces:**
- Produces: `export function selectCoursesByTimetable(courses: Course[], timetableCodes: Set<string>, nowIso: string): Course[]`（変更が無ければ入力と同一参照を返す）
- Consumes: `extractCourseCodes`（`./timetableLink`）、`Course`（`./types`）

- [ ] **Step 1: `Course` に `userToggled?` を追加**

`src/core/types.ts` の `Course` 型に1行追加:

```ts
export type Course = {
  id: string
  name: string
  url: string
  enabled: boolean
  /** ユーザーが手動でON/OFFを切り替えた印。自動選択はこれが立ったコースを触らない。 */
  userToggled?: boolean
  lmsType: 'unknown' | 'letus' | 'moodle' | 'manaba' | 'webclass' | 'generic'
  createdAt: string
  updatedAt: string
}
```

- [ ] **Step 2: 失敗するテストを書く**

`src/core/courseSelect.test.ts` を新規作成:

```ts
import { describe, it, expect } from 'vitest'
import { selectCoursesByTimetable } from './courseSelect'
import type { Course } from './types'

function course(over: Partial<Course>): Course {
  return { id: 'c', name: '', url: '', enabled: false, lmsType: 'letus', createdAt: '', updatedAt: '', ...over }
}
const NOW = '2026-07-08T00:00:00.000Z'

describe('selectCoursesByTimetable', () => {
  it('時間割にコードが一致する未操作コースを自動ONする', () => {
    const courses = [course({ id: 'a', name: '9973337 電気数学', enabled: false })]
    const out = selectCoursesByTimetable(courses, new Set(['9973337']), NOW)
    expect(out[0].enabled).toBe(true)
    expect(out[0].updatedAt).toBe(NOW)
  })
  it('統合コースは片方一致でON', () => {
    const courses = [course({ id: 'a', name: '統合 1111111 / 9973337', enabled: false })]
    const out = selectCoursesByTimetable(courses, new Set(['9973337']), NOW)
    expect(out[0].enabled).toBe(true)
  })
  it('userToggled が立ったコースは触らない', () => {
    const courses = [course({ id: 'a', name: '9973337 電気数学', enabled: false, userToggled: true })]
    const out = selectCoursesByTimetable(courses, new Set(['9973337']), NOW)
    expect(out).toBe(courses)
  })
  it('時間割に無いコードは変更しない', () => {
    const courses = [course({ id: 'a', name: '0000000 他学期', enabled: false })]
    const out = selectCoursesByTimetable(courses, new Set(['9973337']), NOW)
    expect(out).toBe(courses)
  })
  it('既にON・空集合は変更しない（同一参照）', () => {
    const on = [course({ id: 'a', name: '9973337 電気数学', enabled: true })]
    expect(selectCoursesByTimetable(on, new Set(['9973337']), NOW)).toBe(on)
    const c = [course({ id: 'a', name: '9973337 電気数学', enabled: false })]
    expect(selectCoursesByTimetable(c, new Set(), NOW)).toBe(c)
  })
})
```

- [ ] **Step 3: テストが失敗することを確認**

Run: `pnpm vitest run src/core/courseSelect.test.ts`
Expected: FAIL（`courseSelect` module not found）

- [ ] **Step 4: 実装を追加**

`src/core/courseSelect.ts` を新規作成:

```ts
import type { Course } from './types'
import { extractCourseCodes } from './timetableLink'

/**
 * 時間割の科目コード集合に一致する「未操作(userToggled未設定)かつ未ON」のコースを enabled:true にする。
 * 自動DISABLEはしない。変更が無ければ入力と同一参照を返す（呼び出し側で保存をスキップできる）。
 */
export function selectCoursesByTimetable(
  courses: Course[],
  timetableCodes: Set<string>,
  nowIso: string,
): Course[] {
  if (timetableCodes.size === 0) return courses
  let changed = false
  const next = courses.map((c) => {
    if (c.userToggled || c.enabled) return c
    const match = extractCourseCodes(c.name).some((code) => timetableCodes.has(code))
    if (!match) return c
    changed = true
    return { ...c, enabled: true, updatedAt: nowIso }
  })
  return changed ? next : courses
}
```

- [ ] **Step 5: テストが通ることを確認**

Run: `pnpm vitest run src/core/courseSelect.test.ts`
Expected: PASS（5 tests）

- [ ] **Step 6: コミット**

```bash
git add src/core/types.ts src/core/courseSelect.ts src/core/courseSelect.test.ts
git commit -m "feat(core): add selectCoursesByTimetable + Course.userToggled

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: `linkAssignmentsToSlots` に緊急度集計を追加

**Files:**
- Modify: `src/core/timetableLink.ts`
- Test: `src/core/timetableLink.test.ts`（既存に追記）

**Interfaces:**
- Produces（拡張後シグネチャ）:
  ```ts
  export function linkAssignmentsToSlots(
    slots: TimetableSlot[],
    courses: Course[],
    assignments: Assignment[],
    manualAssignments?: ManualAssignment[],
    now?: Date,
  ): {
    assignmentInfo: Record<string, AssignmentSlotInfo>
    courseCodeCounts: Record<string, number>
    courseCodeUrgency: Record<string, DeadlineTier>
  }
  ```
  （`manualAssignments` 既定 `[]`、`now` 既定 `new Date()`。既存呼び出し `linkAssignmentsToSlots(slots, courses, assignments)` はそのまま有効）
- Consumes: `deadlineTier`・`DeadlineTier`（`../utils/date`）、`isSubmittedAssignment`（`../utils/assignment`）、`ManualAssignment`（`./manualAssignment`、type-only）

- [ ] **Step 1: 失敗するテストを追記**

`src/core/timetableLink.test.ts` の末尾に追加（ファイル先頭の import に `linkAssignmentsToSlots` は既にある）:

```ts
import type { ManualAssignment } from './manualAssignment'
import type { DeadlineTier } from '../utils/date'

function assignmentWith(id: string, courseId: string, deadline: string | null, over: Partial<Assignment> = {}): Assignment {
  return { ...assignment(id, courseId), deadline, ...over }
}
function manual(courseName: string, deadline: string, submitted = false): ManualAssignment {
  return { id: 'm' + courseName, courseId: '', courseName, title: '', letusUrl: null, deadline, memo: '', submitted, createdAt: '' }
}

describe('linkAssignmentsToSlots 緊急度', () => {
  const NOW = new Date('2026-07-08T10:00:00+09:00')
  const slots = [slot('mon', 1, '9973337', '445')]
  const courses = [course('a', '9973337 電気数学')]

  it('当日締切の未提出は today', () => {
    const a = [assignmentWith('1', 'a', '2026-07-08T23:00:00+09:00', { submissionStatus: 'not_submitted' })]
    const { courseCodeUrgency } = linkAssignmentsToSlots(slots, courses, a, [], NOW)
    expect(courseCodeUrgency['9973337']).toBe('today')
  })
  it('提出済み・期限切れは除外', () => {
    const a = [
      assignmentWith('1', 'a', '2026-07-08T23:00:00+09:00', { submissionStatus: 'submitted' }),
      assignmentWith('2', 'a', '2026-07-08T23:00:00+09:00', { lifecycleStatus: 'passed' }),
    ]
    const { courseCodeUrgency } = linkAssignmentsToSlots(slots, courses, a, [], NOW)
    expect(courseCodeUrgency['9973337'] ?? 'none').toBe('none')
  })
  it('手動課題(未提出・締切あり)も対象に含める', () => {
    const m = [manual('9973337 電気数学', '2026-07-10T09:00:00+09:00')]
    const { courseCodeUrgency } = linkAssignmentsToSlots(slots, courses, [], m, NOW)
    expect(courseCodeUrgency['9973337']).toBe('week')
  })
  it('today と week が混在したら today を採る', () => {
    const a = [assignmentWith('1', 'a', '2026-07-12T09:00:00+09:00', { submissionStatus: 'not_submitted' })]
    const m = [manual('9973337 電気数学', '2026-07-08T20:00:00+09:00')]
    const { courseCodeUrgency } = linkAssignmentsToSlots(slots, courses, a, m, NOW)
    expect(courseCodeUrgency['9973337']).toBe('today')
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm vitest run src/core/timetableLink.test.ts`
Expected: FAIL（`courseCodeUrgency` is undefined / 5番目引数を受けない）

- [ ] **Step 3: 実装を変更**

`src/core/timetableLink.ts` の先頭importに追加:

```ts
import type { ManualAssignment } from './manualAssignment'
import { deadlineTier, type DeadlineTier } from '../utils/date'
import { isSubmittedAssignment } from '../utils/assignment'
```

`linkAssignmentsToSlots` を置き換え（`courseCodeCounts` は残し、`courseCodeUrgency` を追加）:

```ts
const TIER_RANK: Record<DeadlineTier, number> = { none: 0, week: 1, today: 2 }

export function linkAssignmentsToSlots(
  slots: TimetableSlot[],
  courses: Course[],
  assignments: Assignment[],
  manualAssignments: ManualAssignment[] = [],
  now: Date = new Date(),
): {
  assignmentInfo: Record<string, AssignmentSlotInfo>
  courseCodeCounts: Record<string, number>
  courseCodeUrgency: Record<string, DeadlineTier>
} {
  const courseIdToCodes: Record<string, string[]> = {}
  for (const c of courses) {
    const codes = extractCourseCodes(c.name)
    if (codes.length > 0) courseIdToCodes[c.id] = codes
  }

  const codeToSlot: Record<string, AssignmentSlotInfo> = {}
  for (const s of slots) {
    for (const c of s.classes) {
      if (!c.courseCode) continue
      if (!(c.courseCode in codeToSlot)) {
        codeToSlot[c.courseCode] = {
          day: s.day, period: s.period, room: c.room, isRemote: c.isRemote, courseCode: c.courseCode,
        }
      }
    }
  }

  const assignmentInfo: Record<string, AssignmentSlotInfo> = {}
  const courseCodeCounts: Record<string, number> = {}
  const courseCodeUrgency: Record<string, DeadlineTier> = {}
  for (const s of slots) for (const c of s.classes) if (c.courseCode) {
    courseCodeCounts[c.courseCode] ??= 0
    courseCodeUrgency[c.courseCode] ??= 'none'
  }

  const bump = (code: string, tier: DeadlineTier) => {
    if (TIER_RANK[tier] > TIER_RANK[courseCodeUrgency[code] ?? 'none']) courseCodeUrgency[code] = tier
  }

  for (const a of assignments) {
    const codes = courseIdToCodes[a.courseId]
    if (!codes) continue
    for (const code of codes) if (code in courseCodeCounts) courseCodeCounts[code] += 1
    if (
      a.deadline &&
      !isSubmittedAssignment(a) &&
      a.lifecycleStatus !== 'passed' &&
      a.lifecycleStatus !== 'before_start'
    ) {
      const tier = deadlineTier(a.deadline, now)
      if (tier !== 'none') for (const code of codes) bump(code, tier)
    }
    const matched = codes.find((code) => code in codeToSlot)
    if (matched) assignmentInfo[a.id] = codeToSlot[matched]
  }

  for (const m of manualAssignments) {
    if (m.submitted || !m.deadline) continue
    const tier = deadlineTier(m.deadline, now)
    if (tier === 'none') continue
    for (const code of extractCourseCodes(m.courseName)) bump(code, tier)
  }

  return { assignmentInfo, courseCodeCounts, courseCodeUrgency }
}
```

- [ ] **Step 4: テストが通ることを確認**

Run: `pnpm vitest run src/core/timetableLink.test.ts`
Expected: PASS（既存＋新規4件）

- [ ] **Step 5: コミット**

```bash
git add src/core/timetableLink.ts src/core/timetableLink.test.ts
git commit -m "feat(core): compute per-course deadline urgency in linkAssignmentsToSlots

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: コース自動選択をbackgroundに配線

**Files:**
- Modify: `src/background/index.ts`
- Test: `src/background/index.test.ts`（既存に追記）

**Interfaces:**
- Produces: `export async function applyAutoSelect(now?: Date): Promise<void>`（時間割コードを両学期union→`selectCoursesByTimetable`→変化があれば `saveCourses`）
- Consumes: `getCapturedCourseCodes`（`../core/timetableView`）、`selectCoursesByTimetable`（`../core/courseSelect`）、`academicYear`（`../core/syllabus`）、既存の `getCourses`/`saveCourses`

- [ ] **Step 1: 失敗するテストを追記**

`src/background/index.test.ts`: 冒頭の `chrome` スタブに `storage.onChanged` を、テスト用importに `TABLE_MINIMAL` を足す。まず import 追加（先頭付近）:

```ts
import { TABLE_MINIMAL } from '../core/timetable.fixtures'
```

`vi.stubGlobal('chrome', {...})` の `storage` を次のように拡張（`onChanged` を追加）:

```ts
  storage: {
    local: {
      get: vi.fn(async (keys: string | string[]) => {
        const result: Record<string, unknown> = {}
        const keyList = Array.isArray(keys) ? keys : [keys]
        for (const k of keyList) result[k] = store[k]
        return result
      }),
      set: vi.fn(async (obj: Record<string, unknown>) => { Object.assign(store, obj) }),
    },
    onChanged: { addListener: vi.fn() },
  },
```

`await import('./index')` の分割代入に `applyAutoSelect` を追加。ファイル末尾に describe を追加:

```ts
describe('applyAutoSelect', () => {
  beforeEach(() => { for (const k of Object.keys(store)) delete store[k] })
  it('時間割にあるコースを自動ONし保存する', async () => {
    store[COURSES_KEY] = [makeCourse({ id: 'a', name: '9973337 電気数学', enabled: false })]
    store['timetable:2026:zenki'] = { rawTableHtml: TABLE_MINIMAL, jigenText: '', capturedAt: '2026-07-08T00:00:00.000Z' }
    await applyAutoSelect(new Date('2026-07-08T10:00:00+09:00'))
    const saved = store[COURSES_KEY] as Course[]
    expect(saved[0].enabled).toBe(true)
  })
  it('時間割未取得なら何もしない', async () => {
    store[COURSES_KEY] = [makeCourse({ id: 'a', name: '9973337 電気数学', enabled: false })]
    await applyAutoSelect(new Date('2026-07-08T10:00:00+09:00'))
    const saved = store[COURSES_KEY] as Course[]
    expect(saved[0].enabled).toBe(false)
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm vitest run src/background/index.test.ts`
Expected: FAIL（`applyAutoSelect` is not exported）

- [ ] **Step 3: 実装を追加**

`src/background/index.ts` の先頭import群に追加:

```ts
import { getCapturedCourseCodes } from '../core/timetableView'
import { selectCoursesByTimetable } from '../core/courseSelect'
import { academicYear } from '../core/syllabus'
```

`upsertCourses` 関数の近く（`syncCoursesToServerIfSubscriber` の前後）に追加:

```ts
export async function applyAutoSelect(now: Date = new Date()): Promise<void> {
  const year = academicYear(now)
  const [zenki, kouki] = await Promise.all([
    getCapturedCourseCodes(year, 'zenki'),
    getCapturedCourseCodes(year, 'kouki'),
  ])
  const codes = new Set([...zenki, ...kouki])
  if (codes.size === 0) return
  const courses = await getCourses()
  const next = selectCoursesByTimetable(courses, codes, now.toISOString())
  if (next !== courses) await saveCourses(next)
}
```

`UPSERT_COURSES` ハンドラ（`upsertCourses(courses).then(...)`）を、upsert後に自動選択を走らせるよう変更:

```ts
  if (message?.type === 'UPSERT_COURSES') {
    const courses = (message.courses ?? []) as Course[]
    sendResponse({ ok: true, count: courses.length })
    upsertCourses(courses)
      .then(() => applyAutoSelect())
      .then(() => syncCoursesToServerIfSubscriber(courses))
      .catch((error) => {
        console.error('[LETUS Task Watcher] upsertCourses failed', error)
      })
    return false
  }
```

`chrome.alarms.onAlarm.addListener(...)` の後（トップレベル）に storage 監視を追加:

```ts
chrome.storage.onChanged.addListener((changes, area) => {
  if (area !== 'local') return
  if (Object.keys(changes).some((k) => k.startsWith('timetable:'))) {
    applyAutoSelect().catch((error) => {
      console.error('[LETUS Task Watcher] auto select failed', error)
    })
  }
})
```

- [ ] **Step 4: テストが通ることを確認**

Run: `pnpm vitest run src/background/index.test.ts`
Expected: PASS（既存＋新規2件）

- [ ] **Step 5: コミット**

```bash
git add src/background/index.ts src/background/index.test.ts
git commit -m "feat(ext): auto-select timetable courses on upsert and timetable capture

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: 手動トグルで `userToggled` を記録（App）

**Files:**
- Modify: `src/App.tsx`（`toggleCourse`・`setAllCoursesEnabled`）

**Interfaces:**
- Consumes: `Course.userToggled`（Task 3）

- [ ] **Step 1: `toggleCourse` を変更**

`src/App.tsx` の `toggleCourse` 内、更新オブジェクトに `userToggled: true` を追加:

```ts
      return {
        ...course,
        enabled: !course.enabled,
        userToggled: true,
        updatedAt: new Date().toISOString(),
      }
```

- [ ] **Step 2: `setAllCoursesEnabled` を変更**

`setAllCoursesEnabled` の map 内も同様に:

```ts
      return {
        ...course,
        enabled,
        userToggled: true,
        updatedAt: new Date().toISOString(),
      }
```

- [ ] **Step 3: 型・lintを確認**

Run: `pnpm vitest run src && pnpm lint`
Expected: PASS（型エラー・lintエラーなし）

- [ ] **Step 4: コミット**

```bash
git add src/App.tsx
git commit -m "feat(ext): mark userToggled when user toggles courses

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 7: 時間割セルを緊急度ドット＋NEWバッジに（TimetableSection）

**Files:**
- Modify: `src/components/TimetableSection.tsx`
- Modify: `src/core/timetableLink.ts`（`courseCodeCounts` を削除）
- Test: `src/core/timetableLink.test.ts`（count関連の記述を削除）

**Interfaces:**
- Consumes: `courseCodeUrgency`（Task 4）、新props `manualAssignments: ManualAssignment[]`・`newCodes: string[]`
- Produces（`linkAssignmentsToSlots` の戻り値から `courseCodeCounts` を削除）: `{ assignmentInfo, courseCodeUrgency }`

- [ ] **Step 1: `courseCodeCounts` を削除**

`src/core/timetableLink.ts` から `courseCodeCounts` の宣言・加算・戻り値を削除し、戻り型を `{ assignmentInfo; courseCodeUrgency }` にする。初期化ループは urgency のみ:

```ts
  const courseCodeUrgency: Record<string, DeadlineTier> = {}
  for (const s of slots) for (const c of s.classes) if (c.courseCode) courseCodeUrgency[c.courseCode] ??= 'none'
```

scan課題ループ内の `for (const code of codes) if (code in courseCodeCounts) courseCodeCounts[code] += 1` 行を削除。`return { assignmentInfo, courseCodeUrgency }`。

- [ ] **Step 2: countテストを削除**

`src/core/timetableLink.test.ts` から `courseCodeCounts` を参照する `it(...)` を削除（緊急度テストは残す）。

Run: `pnpm vitest run src/core/timetableLink.test.ts`
Expected: PASS（count参照が無くなり緑）

- [ ] **Step 3: TimetableSection のシグネチャとpropsを変更**

`src/components/TimetableSection.tsx`:

先頭importに追加:
```ts
import type { ManualAssignment } from '../core/manualAssignment'
```

コンポーネント引数を変更:
```ts
export function TimetableSection({ courses, assignments, manualAssignments, newCodes }: {
  courses: Course[]; assignments: Assignment[]; manualAssignments: ManualAssignment[]; newCodes: string[]
}) {
```

`linkAssignmentsToSlots` 呼び出しを変更（`now` はコンポーネント冒頭の `const now = new Date()` を流用）:
```ts
  const { courseCodeUrgency } = useMemo(
    () => linkAssignmentsToSlots(slots, courses, assignments, manualAssignments, now),
    [slots, courses, assignments, manualAssignments, now],
  )
```

- [ ] **Step 4: セルの表示を差し替える**

`timetableCellMeta` 内の
```tsx
{count > 0 && <span className="timetableCount">課題{count}</span>}
```
を削除し、`const count = ...` 行も削除。代わりに（同じ場所に）:
```tsx
{(() => {
  const tier = courseCodeUrgency[c.courseCode] ?? 'none'
  return tier === 'none' ? null : (
    <span
      className={`timetableUrgency ${tier}`}
      title={tier === 'today' ? '当日提出の課題あり' : '今週提出の課題あり'}
      aria-label={tier === 'today' ? '当日提出の課題あり' : '今週提出の課題あり'}
    />
  )
})()}
{newCodes.includes(c.courseCode) && (
  <span className="timetableNew" title="コース内容に更新あり" aria-label="コース内容に更新あり">NEW</span>
)}
```

- [ ] **Step 5: 型・テストを確認**

Run: `pnpm vitest run src && pnpm lint`
Expected: PASS（App.tsx から TimetableSection への props 追加はTask 10で行うため、この時点で `pnpm lint`/`tsc` は App.tsx の呼び出し不足で赤くなる。**Task 10 と連続で実施し、Task 10完了後にまとめて緑を確認する**）

> 注: 本タスク単独では App.tsx の `<TimetableSection courses assignments />` が新propsを満たさず型エラーになる。Task 10（App配線）とペアで実施し、コミットは各タスクで行うが緑確認はTask 10末で行う。

- [ ] **Step 6: コミット**

```bash
git add src/core/timetableLink.ts src/core/timetableLink.test.ts src/components/TimetableSection.tsx
git commit -m "feat(ext): show urgency dot and NEW badge in timetable cells

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 8: 表示日ロジック `resolveDisplayDay`（週末→翌月曜）

**Files:**
- Modify: `src/core/timetableView.ts`
- Test: `src/core/timetableView.test.ts`（Task 2のファイルに追記）

**Interfaces:**
- Produces: `export function resolveDisplayDay(now: Date): { day: DayOfWeek; label: string }`（平日は当日＋label「今日」、土日は `mon`＋label「月曜」）
- Consumes: `DayOfWeek`（`./timetable`）

- [ ] **Step 1: 失敗するテストを追記**

`src/core/timetableView.test.ts` に追加:

```ts
import { resolveDisplayDay } from './timetableView'

describe('resolveDisplayDay', () => {
  it('平日は当日', () => {
    expect(resolveDisplayDay(new Date('2026-07-08T10:00:00+09:00')).day).toBe('wed') // 水曜
    expect(resolveDisplayDay(new Date('2026-07-08T10:00:00+09:00')).label).toBe('今日')
  })
  it('土日は翌月曜', () => {
    expect(resolveDisplayDay(new Date('2026-07-11T10:00:00+09:00')).day).toBe('mon') // 土曜
    expect(resolveDisplayDay(new Date('2026-07-12T10:00:00+09:00')).day).toBe('mon') // 日曜
    expect(resolveDisplayDay(new Date('2026-07-12T10:00:00+09:00')).label).toBe('月曜')
  })
})
```

- [ ] **Step 2: テストが失敗することを確認**

Run: `pnpm vitest run src/core/timetableView.test.ts`
Expected: FAIL（`resolveDisplayDay` is not exported）

- [ ] **Step 3: 実装を追加**

`src/core/timetableView.ts` に追加（`import type { DayOfWeek } from './timetable'` を先頭に足す）:

```ts
const WEEKDAY: Record<number, DayOfWeek | undefined> = { 1: 'mon', 2: 'tue', 3: 'wed', 4: 'thu', 5: 'fri' }

/** ポップアップに表示する曜日を決める。平日は当日、土日は翌月曜。 */
export function resolveDisplayDay(now: Date): { day: DayOfWeek; label: string } {
  const day = WEEKDAY[now.getDay()]
  return day ? { day, label: '今日' } : { day: 'mon', label: '月曜' }
}
```

> 注: `getDay()` はローカルタイムの曜日。拡張は日本のユーザー前提でOSローカル＝JST想定。テストは `+09:00` 付きISOで曜日がJSTと一致するようにしている。

- [ ] **Step 4: テストが通ることを確認**

Run: `pnpm vitest run src/core/timetableView.test.ts`
Expected: PASS

- [ ] **Step 5: コミット**

```bash
git add src/core/timetableView.ts src/core/timetableView.test.ts
git commit -m "feat(core): add resolveDisplayDay (weekend -> next Monday)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 9: `TodayTimetable` コンポーネント（ポップアップ用）

**Files:**
- Create: `src/components/TodayTimetable.tsx`

**Interfaces:**
- Produces: `export function TodayTimetable({ courses, assignments, manualAssignments, newCodes }): JSX`（props型は下記実装参照）
- Consumes: `getTimetableCapture`（`../core/timetableStore`）、`resolveViewSemester`・`loadCourseOverrides`・`resolveDisplayDay`（`../core/timetableView`）、`parseTimetable`（`../core/timetable`）、`applyOverrides`・`linkAssignmentsToSlots`（`../core/timetableLink`）、`academicYear`（`../core/syllabus`）

- [ ] **Step 1: コンポーネントを作成**

`src/components/TodayTimetable.tsx` を新規作成:

```tsx
import { useEffect, useMemo, useState } from 'react'
import type { Course, Assignment } from '../core/types'
import type { ManualAssignment } from '../core/manualAssignment'
import type { TimetableSlot, DayOfWeek } from '../core/timetable'
import { parseTimetable } from '../core/timetable'
import { applyOverrides, linkAssignmentsToSlots } from '../core/timetableLink'
import { getTimetableCapture } from '../core/timetableStore'
import { resolveViewSemester, loadCourseOverrides, resolveDisplayDay } from '../core/timetableView'
import { academicYear } from '../core/syllabus'

const DAY_LABELS: Record<DayOfWeek, string> = { mon: '月', tue: '火', wed: '水', thu: '木', fri: '金', sat: '土' }

export function TodayTimetable({ courses, assignments, manualAssignments, newCodes }: {
  courses: Course[]; assignments: Assignment[]; manualAssignments: ManualAssignment[]; newCodes: string[]
}) {
  const now = new Date()
  const year = academicYear(now)
  const { day, label } = resolveDisplayDay(now)
  const [rawHtml, setRawHtml] = useState<string | null>(null)
  const [overrides, setOverrides] = useState<Record<string, { room?: string }>>({})
  const [loaded, setLoaded] = useState(false)

  useEffect(() => {
    void (async () => {
      const semester = await resolveViewSemester(year, now)
      const cap = await getTimetableCapture(year, semester)
      setRawHtml(cap?.rawTableHtml ?? null)
      setOverrides(await loadCourseOverrides(year, semester, courses))
      setLoaded(true)
    })()
  }, [year, courses])

  const slots: TimetableSlot[] = useMemo(() => {
    if (!rawHtml) return []
    return applyOverrides(parseTimetable(rawHtml), overrides)
  }, [rawHtml, overrides])

  const { courseCodeUrgency } = useMemo(
    () => linkAssignmentsToSlots(slots, courses, assignments, manualAssignments, now),
    [slots, courses, assignments, manualAssignments, now],
  )

  const todayClasses = useMemo(
    () => slots.filter((s) => s.day === day).sort((a, b) => a.period - b.period),
    [slots, day],
  )

  if (!loaded) return null
  if (rawHtml === null) {
    return (
      <section className="todayTimetable">
        <div className="todayTimetableHead">{label}の時間割</div>
        <p className="todayTimetableEmpty">CLASSの「履修 → 学生時間割表」を開くと取り込みます。</p>
      </section>
    )
  }

  return (
    <section className="todayTimetable">
      <div className="todayTimetableHead">{label}（{DAY_LABELS[day]}）の時間割</div>
      {todayClasses.length === 0 ? (
        <p className="todayTimetableEmpty">本日は授業なし</p>
      ) : (
        <ul className="todayTimetableList">
          {todayClasses.map((s) => {
            const c = s.classes[0]
            const tier = courseCodeUrgency[c.courseCode] ?? 'none'
            return (
              <li key={`${s.day}:${s.period}`} className="todayTimetableRow">
                <span className="todayPeriod">{s.period}</span>
                <span className="todayName">{c.name}</span>
                <span className="todayRoom">{c.room}</span>
                {tier !== 'none' && (
                  <span
                    className={`timetableUrgency ${tier}`}
                    title={tier === 'today' ? '当日提出の課題あり' : '今週提出の課題あり'}
                    aria-label={tier === 'today' ? '当日提出の課題あり' : '今週提出の課題あり'}
                  />
                )}
                {newCodes.includes(c.courseCode) && (
                  <span className="timetableNew" title="コース内容に更新あり" aria-label="コース内容に更新あり">NEW</span>
                )}
              </li>
            )
          })}
        </ul>
      )}
    </section>
  )
}
```

- [ ] **Step 2: 型を確認**

Run: `pnpm lint`
Expected: TodayTimetable単体では型エラーなし（未使用importが無いこと）。App配線はTask 10。

- [ ] **Step 3: コミット**

```bash
git add src/components/TodayTimetable.tsx
git commit -m "feat(ext): add TodayTimetable component for popup

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 10: App配線（NEWコード集合・TodayTimetable描画・props受け渡し）

**Files:**
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: `TodayTimetable`（Task 9）、`getUnreadUpdates`（`./background/courseUpdatesStore`）、`extractCourseCodes`（`./core/timetableLink`）、Task 7で変わった `TimetableSection` props

- [ ] **Step 1: import追加**

`src/App.tsx` 先頭付近に追加:

```ts
import { TodayTimetable } from './components/TodayTimetable'
import { getUnreadUpdates } from './background/courseUpdatesStore'
import { extractCourseCodes } from './core/timetableLink'
```

- [ ] **Step 2: NEWコード集合の state と effect**

`const [assignmentSlotMap, ...]` 群の近くに state を追加:

```ts
const [newBadgeCodes, setNewBadgeCodes] = useState<string[]>([])
```

`courses` 変化で未読更新のあるコースの科目コードを集める effect を追加（既存の effect 群の並びに）:

```ts
useEffect(() => {
  let cancelled = false
  void (async () => {
    const codes = new Set<string>()
    for (const course of courses) {
      const unread = await getUnreadUpdates(course.id)
      if (unread.length > 0) for (const code of extractCourseCodes(course.name)) codes.add(code)
    }
    if (!cancelled) setNewBadgeCodes(Array.from(codes))
  })()
  return () => { cancelled = true }
}, [courses])
```

- [ ] **Step 3: ポップアップに TodayTimetable を描画**

`!isDashboard` ブロックの `miniSummary` セクション（`<section className="miniSummary">...</section>`）の直後に追加:

```tsx
<TodayTimetable
  courses={courses}
  assignments={assignments}
  manualAssignments={manualAssignments}
  newCodes={newBadgeCodes}
/>
```

- [ ] **Step 4: ダッシュボードの TimetableSection に新propsを渡す**

`isDashboard` ブロックの `<TimetableSection courses={courses} assignments={assignments} />` を変更:

```tsx
<TimetableSection
  courses={courses}
  assignments={assignments}
  manualAssignments={manualAssignments}
  newCodes={newBadgeCodes}
/>
```

- [ ] **Step 5: 全体の型・テスト・lintを確認（Task 7とまとめて緑確認）**

Run: `pnpm vitest run src && pnpm lint`
Expected: PASS（型エラー・lintエラーなし。Task 7 で入れた TimetableSection の props 不足がここで解消）

- [ ] **Step 6: コミット**

```bash
git add src/App.tsx
git commit -m "feat(ext): wire TodayTimetable and NEW badge codes into App

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 11: ウェルカムガイド本文をCLASS先行フローに改訂

**Files:**
- Modify: `public/welcome.html`

**Interfaces:** なし（静的HTML）

- [ ] **Step 1: ①末尾のnoteを修正**

`public/welcome.html` の
```html
<p class="note">固定すると、ツールバーの拡張機能アイコンから残り課題数がひと目でわかります。</p>
```
を次に置き換え:
```html
<p class="note">固定すると、ワンクリックで「今日の時間割」と課題の締切がひと目で確認できます。</p>
```

- [ ] **Step 2: ②「使い始めの3ステップ」をCLASS先行フローに差し替え**

```html
    <h2>② 使い始めのステップ</h2>
    <ol class="steps">
      <li><strong>CLASSで時間割を取り込む</strong> — CLASSにログインし「履修 → 学生時間割表」を開くと、今学期の時間割を自動で取り込みます</li>
      <li><strong>LETUS を開く</strong> — コースページを開くと、コースを自動登録し、時間割にある科目を自動で追跡対象にします</li>
      <li><strong>今すぐ更新</strong> — ポップアップの「今すぐ更新」で課題を取得。以降は自動でチェックします</li>
    </ol>
    <a class="btn" href="https://class.admin.tus.ac.jp" target="_blank">CLASS を開く →</a>
    <a class="btn" href="https://letus.ed.tus.ac.jp" target="_blank">LETUS を開く →</a>
    <a class="btn secondary" href="index.html#dashboard" target="_blank">ダッシュボードを開く</a>
```

- [ ] **Step 3: 表示確認**

`dist/` を再ビルドせずとも `public/welcome.html` をブラウザで直接開いて崩れが無いことを目視（任意）。ビルド検証はTask 14で行う。

- [ ] **Step 4: コミット**

```bash
git add public/welcome.html
git commit -m "docs(ext): revise welcome guide to CLASS-first auto-select flow

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 12: スタイル（ポップアップ拡大・緊急度ドット・NEWバッジ・今日の時間割）

**Files:**
- Modify: `src/App.css`

**Interfaces:** なし（CSS）。B-1のポップアップ拡大・B-2のドット・B-3のNEW・TodayTimetableの見た目を定義。

- [ ] **Step 1: 既存のポップアップ幅指定を確認して拡大**

`src/App.css` で `.app.popup` または `body` の幅指定（現状のpopup幅）を探し、**約40〜60px広げる**。例: `width: 360px` 相当を `width: 400px` にする（実際の現在値に合わせて調整。ダッシュボード `.app.dashboard` の幅には影響させないこと）。

- [ ] **Step 2: 緊急度ドット・NEWバッジのスタイルを追加**

`src/App.css` 末尾に追加:

```css
.timetableUrgency {
  display: inline-block;
  width: 8px;
  height: 8px;
  border-radius: 50%;
  margin-left: 2px;
  flex: 0 0 auto;
}
.timetableUrgency.today { background: #e53935; }
.timetableUrgency.week { background: #fb8c00; }

.timetableNew {
  display: inline-block;
  font-size: 9px;
  font-weight: 700;
  line-height: 1;
  color: #ffffff;
  background: #1d9e75;
  border-radius: 4px;
  padding: 2px 4px;
  margin-left: 2px;
}
```

- [ ] **Step 3: TodayTimetable のスタイルを追加**

```css
.todayTimetable {
  margin: 8px 0;
  padding: 8px 10px;
  background: var(--card-bg, #ffffff);
  border: 1px solid var(--border, #e5e7eb);
  border-radius: 8px;
}
.todayTimetableHead { font-size: 12px; font-weight: 700; color: #374151; margin-bottom: 6px; }
.todayTimetableEmpty { font-size: 12px; color: #6b7280; }
.todayTimetableList { list-style: none; margin: 0; padding: 0; }
.todayTimetableRow {
  display: flex;
  align-items: center;
  gap: 6px;
  padding: 4px 0;
  font-size: 12px;
  border-bottom: 1px solid #f3f4f6;
}
.todayPeriod {
  flex: 0 0 auto;
  width: 18px;
  text-align: center;
  font-weight: 700;
  color: #7c3aed;
}
.todayName { flex: 1 1 auto; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.todayRoom { flex: 0 0 auto; color: #6b7280; }
```

> 注: ダークテーマ（`[data-theme="dark"]`）で見づらい場合は既存のダーク変数に合わせて色を追加する。既存のダークテーマ定義に倣うこと。

- [ ] **Step 4: 表示確認（ビルド）**

Run: `pnpm build`
Expected: ビルド成功（`dist/` 生成）。`dist/index.html` を拡張機能として読み込み、ポップアップに今日の時間割・ドット・NEWが出ることを目視（可能なら）。

- [ ] **Step 5: コミット**

```bash
git add src/App.css
git commit -m "style(ext): enlarge popup, urgency dot, NEW badge, today timetable

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 13: changelog書き換え＋バージョン bump

**Files:**
- Modify: `public/changelog.html`
- Modify: `manifest.json`（`version` → `1.2.0`）
- Modify: `store-listing.md`（版数表記があれば揃える）

**Interfaces:** なし

- [ ] **Step 1: manifest のバージョンを 1.2.0 に**

`manifest.json` の `"version"` を `"1.2.0"` にする。`host_permissions` に `https://class.admin.tus.ac.jp/*` が含まれていることを確認（既存。無ければ追加）。

- [ ] **Step 2: changelog本文をv1.2.0内容に**

`public/changelog.html` の「今回の変更点」見出しを「v1.2.0にアップデート」に、本文を以下の要点で更新:
- サブスク機能（メモ・優先度・テーマ）を無料開放
- CLASS時間割の取り込み・グリッド表示・科目連携
- シラバスの拡張内表示
- コース内容の更新通知
- **今日の時間割をポップアップに常設／課題の締切を緊急度カラーで表示／更新コースにNEWバッジ**
- **時間割にある科目を自動で追跡対象に（コース自動選択）**
- インストール/更新時のウェルカムガイド

**リタス（Litus）事前登録導線 https://lms.waiteu.dev/app を必ず含める**（既存のロードマップカードがあれば維持）。

- [ ] **Step 3: 確認**

Run: `pnpm build`
Expected: 成功。`dist/changelog.html`・`dist/manifest.json` に反映。

- [ ] **Step 4: コミット**

```bash
git add public/changelog.html manifest.json store-listing.md
git commit -m "chore(release): v1.2.0 changelog and version bump

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 14: リリース品質ゲート＋litus逆流

**Files:**
- Modify: `WORKLOG.md`・`TASKS.md`（記録）
- 逆流先: `C:\dev\litus`（`src/parsers/timetable.ts`・`src/links/syllabus.ts`・`src/updates/courseUpdates.ts` 等、該当があれば）

**Interfaces:** なし（検証と記録）

- [ ] **Step 1: 全テスト緑**

Run: `pnpm vitest run src`
Expected: すべて PASS

- [ ] **Step 2: lint緑**

Run: `pnpm lint`
Expected: エラー0

- [ ] **Step 3: ビルド成功**

Run: `pnpm build`
Expected: `tsc -b` 型エラー0＋`vite build` 成功

- [ ] **Step 4: litus逆流の突き合わせ**

今回追加した純粋ロジックのうち litus に相当機能があるものを突き合わせる:
- `deadlineTier`（`src/utils/date.ts`）・`selectCoursesByTimetable`（`src/core/courseSelect.ts`）は拡張発の新規。litus 側に締切色分け・時間割由来のコース選択があれば良い方に寄せて反映、無ければ逆流不要と記録。
- 既存の timetable/syllabus/courseUpdates 純粋層に変更を加えていないか確認（本計画では未変更のはず）。

`C:\dev\litus` で該当を反映した場合は `pnpm test`（litus）が緑であることを確認。

- [ ] **Step 5: 記録**

`WORKLOG.md` に本リリースの実装内容と逆流の「済み/不要」を、`TASKS.md` のv1.2.0セクション（changelog・逆流タスク）を完了へ更新。

- [ ] **Step 6: コミット**

```bash
git add WORKLOG.md TASKS.md
git commit -m "docs: v1.2.0 worklog and litus reverse-flow record

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

- [ ] **Step 7: 実機疎通（手動・ユーザー環境）**

`dist/` を拡張機能として読み込み、次を確認（実装済み≠検証済みの原則）:
1. 新規インストールでウェルカム（固定→CLASS時間割→LETUS自動選択→更新）が開く
2. CLASSで時間割取得後、時間割にある科目が自動ONになる
3. 手動でOFFにした科目が再取得で再ONされない（userToggled尊重）
4. 時間割セル／今日の時間割に緊急度ドット（赤/橙）とNEWバッジが出る
5. ポップアップに今日の時間割が常設され、週末は翌月曜が出る

（`main` へのマージ・Chrome Web Store申請は本計画の完了後、別途ユーザー判断で実施）

---

## Self-Review

- **Spec coverage**: B-1（Task 8,9,10,12）/B-2（Task 1,4,7,9,12）/B-3（Task 7,9,10,12）/C-1（Task 11）/C-2（Task 3,5,6）/D（Task 13,14）— 全項目にタスク対応あり。
- **Placeholder scan**: 各ステップに実コード・実コマンド・期待結果を明記。「適切に」等の曖昧表現なし。CSSの現行popup幅のみ実測調整（Task 12 Step 1で現在値確認を指示）。
- **Type consistency**: `DeadlineTier`（date.ts）→ timetableLink/TodayTimetable/TimetableSection で一致。`Course.userToggled` → courseSelect/App で一致。`courseCodeUrgency` → Task 4定義、Task 7でcounts削除しApp/2コンポーネントで参照一致。`selectCoursesByTimetable(courses, Set, nowIso)` → background呼び出しと一致。`resolveDisplayDay`/`getCapturedCourseCodes` の戻り型一致。
