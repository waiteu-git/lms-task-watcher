# v1.1.x QA・リリース準備 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `release/v1.1.x` の手動課題追加機能を、バグ修正・ウィジェット表示条件の限定・コースページのインラインバッジ・ダッシュボードのタイムライン統合・通知統合・削除Undoまで仕上げ、Chrome Web Storeにリリースできる品質にする。

**Architecture:** `ManualAssignment` 型に `submitted: boolean` を追加し、LETUSスキャン課題（`Assignment`）と同じ時間軸グルーピングロジックに混在させる。Content Script（`manualTaskWidget.ts`）はコースページと課題提出ページのみで動作するよう制限し、コースページでは各課題リンクへインラインバッジを差し込む。通知は popup側（`App.tsx`）と background側（`runAutoScan`）の両方で手動課題を対象に含める。

**Tech Stack:** React 19 + TypeScript（popup/dashboard）、バニラTS（content script、Vite単一バンドル）、Vitest（ユニットテスト）、chrome.storage.local

## Global Constraints

- 対象ブランチ: `qa/v1.1.x-release`（worktree: `C:\dev\lms-task-watcher-v11-qa`）。`main`・`develop` には影響させない
- Content script（`src/content/courseDetector.ts` と `src/content/manualTaskWidget.ts`）は他のエントリ（`index.html`／`background`）と共有されるモジュールを値としてimportしない。型only importは可。理由: Rollupが共有チャンクを抽出すると、content scriptにESM importが残りクラシックスクリプトとして実行できなくなる（既知のバグでWORKLOG.mdに記録あり）
- 新規ロジックはReactコンポーネントから独立した純粋関数として `src/utils/` または `src/core/` に切り出し、Vitestでユニットテスト可能にする
- 日本語のUI文言・コミットメッセージを維持する
- テスト実行コマンド: `pnpm exec vitest run <path>`（package.jsonに `test` スクリプトなし）

---

## File Structure

| ファイル | 種別 | 責務 |
|---|---|---|
| `src/core/manualAssignment.ts` | 変更 | `ManualAssignment` 型に `submitted` 追加、`toggleManualAssignmentSubmitted` 追加、旧データのマイグレーション |
| `src/core/manualAssignment.test.ts` | 変更 | 上記の追加テスト |
| `src/utils/manualAssignment.ts` | 新規 | 手動課題の時間軸グルーピング純粋関数（`getManualUrgent` 等） |
| `src/utils/manualAssignment.test.ts` | 新規 | 上記のテスト |
| `src/utils/timeline.ts` | 新規 | スキャン課題と手動課題を締切順に混在ソートする `TimelineItem` 型とマージ関数 |
| `src/utils/timeline.test.ts` | 新規 | 上記のテスト |
| `src/components/ManualAssignmentCard.tsx` | 新規 | ダッシュボードの各タイムラインセクション内で表示する手動課題カード（提出トグル・削除ボタン） |
| `src/components/ManualAssignmentSection.tsx` | 削除 | タイムライン統合により不要化 |
| `src/App.tsx` | 変更 | `resetAllData` 修正、タイムライン統合、通知統合、削除Undo追加 |
| `src/App.css` | 変更 | `ManualAssignmentCard` 用スタイル追加（旧 `.manualAssignmentItem` 系を流用・調整） |
| `src/background/index.ts` | 変更 | `checkDeadlineWarningNotifications` に手動課題を統合 |
| `src/content/manualTaskWidget.ts` | 変更 | URL制限、フォームリセットバグ修正、コースページのインラインバッジ、提出トグル |

---

## Task 1: ManualAssignment 型に submitted フィールドを追加

**Files:**
- Modify: `src/core/manualAssignment.ts`
- Test: `src/core/manualAssignment.test.ts`

**Interfaces:**
- Produces: `ManualAssignment.submitted: boolean`、`toggleManualAssignmentSubmitted(id: string): Promise<void>`、`getManualAssignments()` は旧データ（`submitted`フィールド無し）を `submitted: false` として返す

- [ ] **Step 1: 失敗するテストを書く**

`src/core/manualAssignment.test.ts` の `makeAssignment` ヘルパーと末尾に以下を追加する。

```ts
function makeAssignment(overrides?: Partial<ManualAssignment>): ManualAssignment {
  return {
    id: 'test-id-1',
    courseId: 'course-abc',
    courseName: '数理統計学',
    title: 'レポート第5回',
    letusUrl: 'https://letus.ed.tus.ac.jp/mod/forum/discuss.php?d=123',
    deadline: '2026-07-05T23:59:00.000Z',
    memo: '',
    submitted: false,
    createdAt: '2026-06-28T00:00:00.000Z',
    ...overrides,
  }
}

describe('getManualAssignments の submitted マイグレーション', () => {
  it('submitted フィールドが無い旧データは false として扱う', async () => {
    const legacyRecord = {
      id: 'legacy-1',
      courseId: 'course-abc',
      courseName: '数理統計学',
      title: '旧データの課題',
      letusUrl: null,
      deadline: '2026-07-05T23:59:00.000Z',
      memo: '',
      createdAt: '2026-06-28T00:00:00.000Z',
    }

    mockStorage.manualAssignments = [legacyRecord]

    const result = await getManualAssignments()
    expect(result[0].submitted).toBe(false)
  })
})

describe('toggleManualAssignmentSubmitted', () => {
  it('submitted を true から false へ反転できる', async () => {
    await addManualAssignment(makeAssignment({ id: 'toggle-1', submitted: false }))
    await toggleManualAssignmentSubmitted('toggle-1')
    const result = await getManualAssignments()
    expect(result.find((a) => a.id === 'toggle-1')?.submitted).toBe(true)
  })

  it('再度呼ぶと false に戻る', async () => {
    await addManualAssignment(makeAssignment({ id: 'toggle-2', submitted: false }))
    await toggleManualAssignmentSubmitted('toggle-2')
    await toggleManualAssignmentSubmitted('toggle-2')
    const result = await getManualAssignments()
    expect(result.find((a) => a.id === 'toggle-2')?.submitted).toBe(false)
  })

  it('存在しないIDを指定しても壊れない', async () => {
    await addManualAssignment(makeAssignment({ id: 'keep-1', submitted: false }))
    await toggleManualAssignmentSubmitted('non-existent')
    const result = await getManualAssignments()
    expect(result).toHaveLength(1)
    expect(result[0].submitted).toBe(false)
  })
})
```

末尾のimport文に `toggleManualAssignmentSubmitted` を追加する。

```ts
import {
  getManualAssignments,
  addManualAssignment,
  deleteManualAssignment,
  toggleManualAssignmentSubmitted,
  type ManualAssignment,
} from './manualAssignment'
```

- [ ] **Step 2: テストが失敗することを確認する**

Run: `pnpm exec vitest run src/core/manualAssignment.test.ts`
Expected: FAIL（`toggleManualAssignmentSubmitted` が存在しない、`submitted` 未定義によりマイグレーションテストも失敗）

- [ ] **Step 3: 実装する**

`src/core/manualAssignment.ts` を以下の内容に置き換える。

```ts
import { MANUAL_ASSIGNMENTS_KEY } from '../background/storageKeys'

export type ManualAssignment = {
  id: string
  courseId: string
  courseName: string
  title: string
  letusUrl: string | null
  deadline: string
  memo: string
  submitted: boolean
  createdAt: string
}

type LegacyManualAssignment = Omit<ManualAssignment, 'submitted'> & {
  submitted?: boolean
}

type ManualAssignmentsStorage = {
  manualAssignments?: LegacyManualAssignment[]
}

export async function getManualAssignments(): Promise<ManualAssignment[]> {
  const result = (await chrome.storage.local.get(
    MANUAL_ASSIGNMENTS_KEY,
  )) as ManualAssignmentsStorage

  const records = result.manualAssignments ?? []

  return records.map((record) => ({
    ...record,
    submitted: record.submitted ?? false,
  }))
}

export async function saveManualAssignments(
  items: ManualAssignment[],
): Promise<void> {
  await chrome.storage.local.set({ [MANUAL_ASSIGNMENTS_KEY]: items })
}

export async function addManualAssignment(
  item: ManualAssignment,
): Promise<void> {
  const current = await getManualAssignments()
  await saveManualAssignments([...current, item])
}

export async function deleteManualAssignment(id: string): Promise<void> {
  const current = await getManualAssignments()
  await saveManualAssignments(current.filter((a) => a.id !== id))
}

export async function toggleManualAssignmentSubmitted(id: string): Promise<void> {
  const current = await getManualAssignments()
  const updated = current.map((a) =>
    a.id === id ? { ...a, submitted: !a.submitted } : a,
  )
  await saveManualAssignments(updated)
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `pnpm exec vitest run src/core/manualAssignment.test.ts`
Expected: PASS（全テスト）

- [ ] **Step 5: コミット**

```bash
git add src/core/manualAssignment.ts src/core/manualAssignment.test.ts
git commit -m "feat(core): 手動課題にsubmittedフィールドとトグル関数を追加"
```

---

## Task 2: 手動課題の時間軸グルーピング純粋関数を追加

**Files:**
- Create: `src/utils/manualAssignment.ts`
- Test: `src/utils/manualAssignment.test.ts`

**Interfaces:**
- Consumes: `ManualAssignment`（Task 1で `submitted` 追加済み）、`isWithin24Hours` / `isTomorrowOrEarlierAfter24Hours` / `isWithinThisWeekAfterTomorrow` / `isLaterThanThisWeek`（`src/utils/date.ts` 既存）
- Produces: `sortManualByDeadline(a, b): number`、`getManualUrgent(items): ManualAssignment[]`、`getManualTomorrow(items): ManualAssignment[]`、`getManualThisWeek(items): ManualAssignment[]`、`getManualLater(items): ManualAssignment[]`、`getManualSubmitted(items): ManualAssignment[]`

- [ ] **Step 1: 失敗するテストを書く**

`src/utils/manualAssignment.test.ts` を新規作成する。

```ts
import { describe, it, expect } from 'vitest'
import type { ManualAssignment } from '../core/manualAssignment'
import {
  sortManualByDeadline,
  getManualUrgent,
  getManualTomorrow,
  getManualThisWeek,
  getManualLater,
  getManualSubmitted,
} from './manualAssignment'

function makeAssignment(overrides?: Partial<ManualAssignment>): ManualAssignment {
  return {
    id: 'm-1',
    courseId: 'course-abc',
    courseName: '数理統計学',
    title: '手動課題',
    letusUrl: null,
    deadline: new Date(Date.now() + 60_000).toISOString(),
    memo: '',
    submitted: false,
    createdAt: '2026-06-28T00:00:00.000Z',
    ...overrides,
  }
}

function hoursFromNow(hours: number): string {
  return new Date(Date.now() + hours * 60 * 60 * 1000).toISOString()
}

describe('sortManualByDeadline', () => {
  it('締切が早い順に並ぶ', () => {
    const a = makeAssignment({ id: 'a', deadline: hoursFromNow(5) })
    const b = makeAssignment({ id: 'b', deadline: hoursFromNow(1) })
    const sorted = [a, b].sort(sortManualByDeadline)
    expect(sorted.map((x) => x.id)).toEqual(['b', 'a'])
  })
})

describe('getManualUrgent', () => {
  it('24時間以内かつ未提出のみ含む', () => {
    const within = makeAssignment({ id: 'within', deadline: hoursFromNow(2) })
    const outside = makeAssignment({ id: 'outside', deadline: hoursFromNow(48) })
    const submitted = makeAssignment({ id: 'submitted', deadline: hoursFromNow(2), submitted: true })
    const result = getManualUrgent([within, outside, submitted])
    expect(result.map((x) => x.id)).toEqual(['within'])
  })
})

describe('getManualTomorrow', () => {
  it('24時間より後〜明日いっぱいの未提出課題を含む', () => {
    const item = makeAssignment({ id: 'tomorrow', deadline: hoursFromNow(30) })
    const tooFar = makeAssignment({ id: 'far', deadline: hoursFromNow(24 * 10) })
    const result = getManualTomorrow([item, tooFar])
    expect(result.map((x) => x.id)).toEqual(['tomorrow'])
  })
})

describe('getManualThisWeek', () => {
  it('明日より後〜7日以内の未提出課題を含む', () => {
    const item = makeAssignment({ id: 'thisweek', deadline: hoursFromNow(24 * 5) })
    const result = getManualThisWeek([item])
    expect(result.map((x) => x.id)).toEqual(['thisweek'])
  })
})

describe('getManualLater', () => {
  it('7日より後の未提出課題を含む', () => {
    const item = makeAssignment({ id: 'later', deadline: hoursFromNow(24 * 10) })
    const result = getManualLater([item])
    expect(result.map((x) => x.id)).toEqual(['later'])
  })
})

describe('getManualSubmitted', () => {
  it('submitted が true の課題のみ含む', () => {
    const submitted = makeAssignment({ id: 'done', submitted: true, deadline: hoursFromNow(-5) })
    const notSubmitted = makeAssignment({ id: 'pending', submitted: false })
    const result = getManualSubmitted([submitted, notSubmitted])
    expect(result.map((x) => x.id)).toEqual(['done'])
  })
})
```

- [ ] **Step 2: テストが失敗することを確認する**

Run: `pnpm exec vitest run src/utils/manualAssignment.test.ts`
Expected: FAIL（`src/utils/manualAssignment.ts` が存在しない）

- [ ] **Step 3: 実装する**

`src/utils/manualAssignment.ts` を新規作成する。

```ts
import type { ManualAssignment } from '../core/manualAssignment'
import {
  isWithin24Hours,
  isTomorrowOrEarlierAfter24Hours,
  isWithinThisWeekAfterTomorrow,
  isLaterThanThisWeek,
} from './date'

export function sortManualByDeadline(
  a: ManualAssignment,
  b: ManualAssignment,
): number {
  return new Date(a.deadline).getTime() - new Date(b.deadline).getTime()
}

export function getManualUrgent(items: ManualAssignment[]): ManualAssignment[] {
  return items
    .filter((item) => !item.submitted && isWithin24Hours(item.deadline))
    .sort(sortManualByDeadline)
}

export function getManualTomorrow(items: ManualAssignment[]): ManualAssignment[] {
  return items
    .filter((item) => !item.submitted && isTomorrowOrEarlierAfter24Hours(item.deadline))
    .sort(sortManualByDeadline)
}

export function getManualThisWeek(items: ManualAssignment[]): ManualAssignment[] {
  return items
    .filter((item) => !item.submitted && isWithinThisWeekAfterTomorrow(item.deadline))
    .sort(sortManualByDeadline)
}

export function getManualLater(items: ManualAssignment[]): ManualAssignment[] {
  return items
    .filter((item) => !item.submitted && isLaterThanThisWeek(item.deadline))
    .sort(sortManualByDeadline)
}

export function getManualSubmitted(items: ManualAssignment[]): ManualAssignment[] {
  return items.filter((item) => item.submitted).sort(sortManualByDeadline)
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `pnpm exec vitest run src/utils/manualAssignment.test.ts`
Expected: PASS（全テスト）

- [ ] **Step 5: コミット**

```bash
git add src/utils/manualAssignment.ts src/utils/manualAssignment.test.ts
git commit -m "feat(utils): 手動課題の時間軸グルーピング関数を追加"
```

---

## Task 3: スキャン課題と手動課題を混在ソートする timeline ユーティリティ

**Files:**
- Create: `src/utils/timeline.ts`
- Test: `src/utils/timeline.test.ts`

**Interfaces:**
- Consumes: `Assignment`（`src/core/types.ts`）、`ManualAssignment`（`src/core/manualAssignment.ts`）
- Produces: `type TimelineItem = { kind: 'scan'; assignment: Assignment } | { kind: 'manual'; assignment: ManualAssignment }`、`mergeTimeline(scanItems: Assignment[], manualItems: ManualAssignment[]): TimelineItem[]`（締切昇順にマージ）

- [ ] **Step 1: 失敗するテストを書く**

`src/utils/timeline.test.ts` を新規作成する。

```ts
import { describe, it, expect } from 'vitest'
import type { Assignment } from '../core/types'
import type { ManualAssignment } from '../core/manualAssignment'
import { mergeTimeline } from './timeline'

function makeAssignment(overrides?: Partial<Assignment>): Assignment {
  return {
    id: 'scan-1',
    courseId: 'course-abc',
    courseName: '数理統計学',
    title: 'スキャン課題',
    url: 'https://letus.ed.tus.ac.jp/mod/assign/view.php?id=1',
    deadline: '2026-07-05T12:00:00.000Z',
    deadlineText: '',
    sourceText: '',
    submissionStatus: 'not_submitted',
    lifecycleStatus: 'active',
    detectedAt: '2026-06-01T00:00:00.000Z',
    firstSeenAt: '2026-06-01T00:00:00.000Z',
    lastSeenAt: '2026-06-01T00:00:00.000Z',
    lastCheckedAt: '2026-06-01T00:00:00.000Z',
    ...overrides,
  }
}

function makeManual(overrides?: Partial<ManualAssignment>): ManualAssignment {
  return {
    id: 'manual-1',
    courseId: 'course-abc',
    courseName: '数理統計学',
    title: '手動課題',
    letusUrl: null,
    deadline: '2026-07-05T06:00:00.000Z',
    memo: '',
    submitted: false,
    createdAt: '2026-06-28T00:00:00.000Z',
    ...overrides,
  }
}

describe('mergeTimeline', () => {
  it('締切が早い順に scan/manual を混在させる', () => {
    const scan = makeAssignment({ id: 'scan-1', deadline: '2026-07-05T12:00:00.000Z' })
    const manual = makeManual({ id: 'manual-1', deadline: '2026-07-05T06:00:00.000Z' })
    const result = mergeTimeline([scan], [manual])
    expect(result.map((item) => item.assignment.id)).toEqual(['manual-1', 'scan-1'])
    expect(result.map((item) => item.kind)).toEqual(['manual', 'scan'])
  })

  it('空配列同士でも壊れない', () => {
    expect(mergeTimeline([], [])).toEqual([])
  })

  it('scan のみでも manual のみでも動く', () => {
    const scan = makeAssignment()
    expect(mergeTimeline([scan], [])).toHaveLength(1)
    const manual = makeManual()
    expect(mergeTimeline([], [manual])).toHaveLength(1)
  })
})
```

- [ ] **Step 2: テストが失敗することを確認する**

Run: `pnpm exec vitest run src/utils/timeline.test.ts`
Expected: FAIL（`src/utils/timeline.ts` が存在しない）

- [ ] **Step 3: 実装する**

`src/utils/timeline.ts` を新規作成する。

```ts
import type { Assignment } from '../core/types'
import type { ManualAssignment } from '../core/manualAssignment'

export type TimelineItem =
  | { kind: 'scan'; assignment: Assignment }
  | { kind: 'manual'; assignment: ManualAssignment }

function getDeadlineTime(item: TimelineItem): number {
  const deadline = item.assignment.deadline
  return deadline ? new Date(deadline).getTime() : Number.POSITIVE_INFINITY
}

export function mergeTimeline(
  scanItems: Assignment[],
  manualItems: ManualAssignment[],
): TimelineItem[] {
  const items: TimelineItem[] = [
    ...scanItems.map((assignment): TimelineItem => ({ kind: 'scan', assignment })),
    ...manualItems.map((assignment): TimelineItem => ({ kind: 'manual', assignment })),
  ]

  return items.sort((a, b) => getDeadlineTime(a) - getDeadlineTime(b))
}
```

- [ ] **Step 4: テストが通ることを確認する**

Run: `pnpm exec vitest run src/utils/timeline.test.ts`
Expected: PASS（全テスト）

- [ ] **Step 5: コミット**

```bash
git add src/utils/timeline.ts src/utils/timeline.test.ts
git commit -m "feat(utils): スキャン課題と手動課題を締切順に混在させるtimelineユーティリティを追加"
```

---

## Task 4: ManualAssignmentCard コンポーネントを作成

**Files:**
- Create: `src/components/ManualAssignmentCard.tsx`
- Modify: `src/App.css`（カードスタイル追加）

**Interfaces:**
- Consumes: `ManualAssignment`（Task 1）
- Produces: `ManualAssignmentCard({ assignment: ManualAssignment, onToggleSubmitted: (id: string) => void, onDelete: (id: string) => void }): JSX.Element`

このコンポーネントはAssignmentCardと並べて各タイムラインセクション内に表示される。ユニットテストはReact Testing Libraryが未導入のためスキップし、Task 8でApp.tsx統合後に手動確認する。

- [ ] **Step 1: コンポーネントを実装する**

`src/components/ManualAssignmentCard.tsx` を新規作成する。

```tsx
import type { ManualAssignment } from '../core/manualAssignment'
import { formatDeadline } from '../utils/date'

export function ManualAssignmentCard({
  assignment,
  onToggleSubmitted,
  onDelete,
}: {
  assignment: ManualAssignment
  onToggleSubmitted: (id: string) => void
  onDelete: (id: string) => void
}) {
  return (
    <article className="manualCard">
      <div className="manualCardTop">
        <span className="dateText">{formatDeadline(assignment.deadline)}</span>
        <span className="manualBadge">手動</span>
      </div>

      <div className="manualCardTitle">
        {assignment.letusUrl ? (
          <a
            href={assignment.letusUrl}
            target="_blank"
            rel="noreferrer"
            className="manualAssignmentLink"
          >
            {assignment.title}
          </a>
        ) : (
          <span>{assignment.title}</span>
        )}
      </div>

      <div className="manualCardMeta">{assignment.courseName}</div>

      {assignment.memo && <div className="manualCardMemo">{assignment.memo}</div>}

      <div className="manualCardFooter">
        <button
          type="button"
          className={`manualSubmitToggle ${assignment.submitted ? 'submitted' : ''}`}
          onClick={() => onToggleSubmitted(assignment.id)}
        >
          {assignment.submitted ? '✓ 提出済み' : '○ 未提出'}
        </button>

        <button
          type="button"
          className="manualDeleteBtn"
          onClick={() => onDelete(assignment.id)}
          aria-label={`${assignment.title}を削除`}
        >
          削除
        </button>
      </div>
    </article>
  )
}
```

- [ ] **Step 2: スタイルを追加する**

`src/App.css` の `.manualDeleteBtn:hover { ... }` ブロック（1286〜1288行付近）の直後に以下を追加する。

```css
.manualCard {
  display: grid;
  gap: 4px;
  padding: 10px 12px;
  border-radius: 14px;
  background: var(--color-surface, #fff);
  border: 1px solid var(--color-border, #e5e7eb);
  margin-bottom: 8px;
}

.manualCardTop {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.manualCardTitle {
  font-size: 13px;
  font-weight: 700;
  color: var(--color-text);
}

.manualCardMeta {
  font-size: 11px;
  color: #6b7280;
}

.manualCardMemo {
  font-size: 11px;
  color: #6b7280;
}

.manualCardFooter {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-top: 4px;
}

.manualSubmitToggle {
  font-size: 11px;
  font-weight: 700;
  padding: 3px 8px;
  border-radius: 999px;
  border: 1px solid var(--color-border, #e5e7eb);
  background: var(--color-surface, #fff);
  color: #6b7280;
  cursor: pointer;
}

.manualSubmitToggle.submitted {
  background: #d1fae5;
  border-color: #10b981;
  color: #065f46;
}
```

- [ ] **Step 3: ビルドが通ることを確認する**

Run: `pnpm exec tsc -b --noEmit`
Expected: エラーなし（既存App.tsxはまだ`ManualAssignmentSection`を参照しているため、この時点ではTask 8完了までビルド全体が通らなくても良い。`ManualAssignmentCard.tsx` 単体に構文・型エラーが無いことを目視確認する）

- [ ] **Step 4: コミット**

```bash
git add src/components/ManualAssignmentCard.tsx src/App.css
git commit -m "feat(ui): タイムライン統合用のManualAssignmentCardコンポーネントを追加"
```

---

## Task 5: resetAllData のバグ修正

**Files:**
- Modify: `src/App.tsx:788-809`

**Interfaces:**
- Consumes: `MANUAL_ASSIGNMENTS_KEY`（`src/background/storageKeys.ts`、既にApp.tsxでimport済み）

- [ ] **Step 1: 現状コードを確認する**

`src/App.tsx` の `resetAllData` 関数（788行目付近）は以下のようになっている。

```ts
  async function resetAllData() {
    await clearAssignments()
    await clearAssignmentCandidates()
    await clearCourses()
    await chrome.storage.local.remove([
      ASSIGNMENT_SCAN_STATUS_KEY,
      DEADLINE_SCAN_STATUS_KEY,
      LAST_REFRESH_AT_KEY,
      LAST_STALE_NOTIFICATION_AT_KEY,
      IGNORED_ASSIGNMENT_IDS_KEY,
      NOTIFIED_DEADLINE_KEYS_KEY,
    ])

    setAssignments([])
    setCourses([])
    setIgnoredAssignmentIds([])
    setLastHiddenAssignment(null)
    setAssignmentScanStatus(initialAssignmentScanStatus)
    setDeadlineScanStatus(initialDeadlineScanStatus)
    setLastRefreshAt(null)
    setMessage('保存データをすべて初期化しました。')
  }
```

`MANUAL_ASSIGNMENTS_KEY` が `chrome.storage.local.remove` の配列に含まれていない。

- [ ] **Step 2: 修正する**

`resetAllData` を以下に置き換える。

```ts
  async function resetAllData() {
    await clearAssignments()
    await clearAssignmentCandidates()
    await clearCourses()
    await chrome.storage.local.remove([
      ASSIGNMENT_SCAN_STATUS_KEY,
      DEADLINE_SCAN_STATUS_KEY,
      LAST_REFRESH_AT_KEY,
      LAST_STALE_NOTIFICATION_AT_KEY,
      IGNORED_ASSIGNMENT_IDS_KEY,
      NOTIFIED_DEADLINE_KEYS_KEY,
      MANUAL_ASSIGNMENTS_KEY,
    ])

    setAssignments([])
    setCourses([])
    setIgnoredAssignmentIds([])
    setLastHiddenAssignment(null)
    setAssignmentScanStatus(initialAssignmentScanStatus)
    setDeadlineScanStatus(initialDeadlineScanStatus)
    setLastRefreshAt(null)
    setManualAssignments([])
    setMessage('保存データをすべて初期化しました。')
  }
```

- [ ] **Step 3: 型チェックを通す**

Run: `pnpm exec tsc -b --noEmit`
Expected: エラーなし（`MANUAL_ASSIGNMENTS_KEY` は既にimport済み、`setManualAssignments` は既存state setter）

- [ ] **Step 4: コミット**

```bash
git add src/App.tsx
git commit -m "fix(dashboard): すべての保存データ初期化で手動課題も削除されるよう修正"
```

---

## Task 6: 削除Undo機能の追加（先行：ロジックのみ）

**Files:**
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: `addManualAssignment`（`src/core/manualAssignment.ts`）
- Produces: `lastDeletedManualAssignment` state、`handleDeleteManualAssignment(id: string): Promise<void>`（既存を更新）、`undoLastDeletedManualAssignment(): Promise<void>`

- [ ] **Step 1: state を追加する**

`src/App.tsx` の `lastHiddenAssignment` state定義（87-88行目付近）の直後に追加する。

```ts
  const [lastHiddenAssignment, setLastHiddenAssignment] =
    useState<Assignment | null>(null)
  const [lastDeletedManualAssignment, setLastDeletedManualAssignment] =
    useState<ManualAssignment | null>(null)
```

- [ ] **Step 2: handleDeleteManualAssignment を更新する**

既存の `handleDeleteManualAssignment`（755-758行目付近）を以下に置き換える。

```ts
  async function handleDeleteManualAssignment(id: string) {
    const target = manualAssignments.find((a) => a.id === id)
    await deleteManualAssignment(id)
    setManualAssignments((prev) => prev.filter((a) => a.id !== id))
    setLastDeletedManualAssignment(target ?? null)
    setMessage('手動課題を削除しました。')
  }

  async function undoLastDeletedManualAssignment() {
    if (!lastDeletedManualAssignment) {
      return
    }

    await addManualAssignment(lastDeletedManualAssignment)
    setManualAssignments((prev) => [...prev, lastDeletedManualAssignment])
    setLastDeletedManualAssignment(null)
    setMessage('手動課題を元に戻しました。')
  }
```

- [ ] **Step 3: Undoトーストを追加する**

既存の `lastHiddenAssignment` 用Undoトースト（857-865行目付近）の直後に追加する。

```tsx
      {lastDeletedManualAssignment && (
        <div className="undoToast">
          <span>「{lastDeletedManualAssignment.title}」を削除しました。</span>

          <button
            type="button"
            onClick={() => void undoLastDeletedManualAssignment()}
          >
            元に戻す
          </button>
        </div>
      )}
```

- [ ] **Step 4: 型チェックを通す**

Run: `pnpm exec tsc -b --noEmit`
Expected: エラーなし

- [ ] **Step 5: コミット**

```bash
git add src/App.tsx
git commit -m "feat(dashboard): 手動課題削除にUndoトーストを追加"
```

---

## Task 7: 通知統合（popup側）

**Files:**
- Modify: `src/App.tsx`

**Interfaces:**
- Consumes: `getManualUrgent` は使わず、`checkDeadlineWarningNotifications` 内で直接 `ManualAssignment[]` をフィルタする（時間窓は1h/3h/24hの個別判定が必要なため Task 2 のバケツ関数は流用しない）

- [ ] **Step 1: checkDeadlineWarningNotifications のシグネチャを変更する**

`src/App.tsx` の `checkDeadlineWarningNotifications` 関数（133-210行目付近）を以下に置き換える。第4引数 `sourceManualAssignments` を追加し、ループ対象に手動課題を統合する。

```ts
  async function checkDeadlineWarningNotifications(
    sourceAssignments: Assignment[],
    sourceCourses: Course[],
    sourceIgnoredIds: string[],
    sourceManualAssignments: ManualAssignment[],
  ) {
    const ignoredSet = new Set(sourceIgnoredIds)
    const notifiedKeys = await getNotifiedDeadlineKeys()
    const notifiedSet = new Set(notifiedKeys)
    const nextNotifiedKeys = new Set(notifiedKeys)
    let changed = false

    const visibleTargets = sourceAssignments
      .filter((assignment) => {
        return (
          !ignoredSet.has(assignment.id) &&
          isAssignmentVisibleByCourse(assignment, sourceCourses) &&
          assignment.deadline &&
          !isSubmittedAssignment(assignment) &&
          assignment.lifecycleStatus !== 'passed'
        )
      })
      .sort(sortByDeadline)

    const manualTargets = sourceManualAssignments.filter(
      (assignment) => !assignment.submitted && assignment.deadline,
    )

    type NotifyTarget = { id: string; title: string; courseName: string; deadline: string }

    const allTargets: NotifyTarget[] = [
      ...visibleTargets
        .filter((a): a is Assignment & { deadline: string } => a.deadline !== null)
        .map((a) => ({ id: a.id, title: a.title, courseName: a.courseName, deadline: a.deadline })),
      ...manualTargets.map((a) => ({ id: a.id, title: a.title, courseName: a.courseName, deadline: a.deadline })),
    ]

    for (const target of allTargets) {
      const diff = new Date(target.deadline).getTime() - Date.now()

      if (diff <= 0) {
        continue
      }

      const oneHourKey = `${target.id}:1h`
      const threeHourKey = `${target.id}:3h`
      const oneDayKey = `${target.id}:24h`

      if (diff <= ONE_HOUR_MS && !notifiedSet.has(oneHourKey)) {
        createNotification(
          `letus-task-watcher-deadline-1h-${target.id}-${Date.now()}`,
          '締切まで1時間以内',
          `${target.title}\n${target.courseName}`,
        )

        nextNotifiedKeys.add(oneHourKey)
        changed = true
        continue
      }

      if (diff <= THREE_HOURS_MS && !notifiedSet.has(threeHourKey)) {
        createNotification(
          `letus-task-watcher-deadline-3h-${target.id}-${Date.now()}`,
          '締切まで3時間以内',
          `${target.title}\n${target.courseName}`,
        )

        nextNotifiedKeys.add(threeHourKey)
        changed = true
        continue
      }

      if (diff <= ONE_DAY_MS && !notifiedSet.has(oneDayKey)) {
        createNotification(
          `letus-task-watcher-deadline-24h-${target.id}-${Date.now()}`,
          '締切まで24時間以内',
          `${target.title}\n${target.courseName}`,
        )

        nextNotifiedKeys.add(oneDayKey)
        changed = true
      }
    }

    if (changed) {
      await saveNotifiedDeadlineKeys(Array.from(nextNotifiedKeys))
    }
  }
```

- [ ] **Step 2: 呼び出し元を更新する**

`checkDeadlineWarningNotifications` の呼び出し箇所は3箇所ある。それぞれ第4引数を追加する。

1. `useEffect` 内（283-300行目付近）:

```ts
  useEffect(() => {
    if (hasCheckedDeadlineNotificationRef.current) {
      return
    }

    hasCheckedDeadlineNotificationRef.current = true

    void (async () => {
      const savedAssignments = await getAssignments()
      const savedCourses = await getCourses()
      const savedIgnoredIds = await getIgnoredAssignmentIds()
      const savedManualAssignments = await getManualAssignments()

      await checkDeadlineWarningNotifications(
        savedAssignments,
        savedCourses,
        savedIgnoredIds,
        savedManualAssignments,
      )
    })()
  }, [])
```

2. `updateNow` 内（388-392行目付近）:

```ts
      await checkDeadlineWarningNotifications(
        latestAssignments,
        latestCourses,
        latestIgnoredIds,
        manualAssignments,
      )
```

(`updateNow` は `manualAssignments` stateを参照できるので新規取得は不要。ただし `useCallback` の依存配列に `manualAssignments` を追加する必要がある。`updateNow` 定義の末尾、`}, [showOnboarding])` を `}, [showOnboarding, manualAssignments])` に変更する。)

- [ ] **Step 3: 型チェックを通す**

Run: `pnpm exec tsc -b --noEmit`
Expected: エラーなし

- [ ] **Step 4: コミット**

```bash
git add src/App.tsx
git commit -m "feat(dashboard): 締切通知に手動課題を統合"
```

---

## Task 8: 通知統合（background側）

**Files:**
- Modify: `src/background/index.ts`

**Interfaces:**
- Consumes: `getManualAssignments`（`src/core/manualAssignment.ts`、background/index.tsから値importしても問題ない。理由: background.jsは独立エントリで他エントリと共有チャンクの心配がない。content scriptのみ制約対象）

- [ ] **Step 1: import を追加する**

`src/background/index.ts` の冒頭のimport群（1-18行目）に追加する。

```ts
import { getManualAssignments } from '../core/manualAssignment'
```

- [ ] **Step 2: checkDeadlineWarningNotifications を更新する**

`checkDeadlineWarningNotifications` 関数（780-845行目付近）を以下に置き換える。

```ts
async function checkDeadlineWarningNotifications(): Promise<void> {
  const [assignments, ignoredIds, notifiedKeys, manualAssignments] = await Promise.all([
    getAssignments(),
    getIgnoredAssignmentIds(),
    getNotifiedDeadlineKeys(),
    getManualAssignments(),
  ])

  const ignoredSet = new Set(ignoredIds)
  const notifiedSet = new Set(notifiedKeys)
  const nextNotifiedKeys = new Set(notifiedKeys)
  let changed = false

  const scanTargets = assignments.filter(
    (a) =>
      !ignoredSet.has(a.id) &&
      a.deadline !== null &&
      a.lifecycleStatus !== 'passed' &&
      a.lifecycleStatus !== 'submitted' &&
      a.submissionStatus !== 'submitted' &&
      a.submissionStatus !== 'completed',
  )

  const manualTargets = manualAssignments.filter((a) => !a.submitted)

  type NotifyTarget = { id: string; title: string; courseName: string; deadline: string; url?: string }

  const allTargets: NotifyTarget[] = [
    ...scanTargets
      .filter((a): a is Assignment & { deadline: string } => a.deadline !== null)
      .map((a) => ({ id: a.id, title: a.title, courseName: a.courseName, deadline: a.deadline, url: a.url })),
    ...manualTargets.map((a) => ({
      id: a.id,
      title: a.title,
      courseName: a.courseName,
      deadline: a.deadline,
      url: a.letusUrl ?? undefined,
    })),
  ]

  for (const target of allTargets) {
    const diff = new Date(target.deadline).getTime() - Date.now()
    if (diff <= 0) continue

    const key1h = `${target.id}:1h`
    const key3h = `${target.id}:3h`
    const key24h = `${target.id}:24h`

    if (diff <= ONE_HOUR_MS && !notifiedSet.has(key1h)) {
      await createNotification({
        id: `task-watcher-deadline-1h-${target.id}-${Date.now()}`,
        title: '締切まで1時間以内',
        message: `${target.title}\n${target.courseName}`,
        url: target.url,
      })
      nextNotifiedKeys.add(key1h)
      changed = true
    } else if (diff <= THREE_HOURS_MS && !notifiedSet.has(key3h)) {
      await createNotification({
        id: `task-watcher-deadline-3h-${target.id}-${Date.now()}`,
        title: '締切まで3時間以内',
        message: `${target.title}\n${target.courseName}`,
        url: target.url,
      })
      nextNotifiedKeys.add(key3h)
      changed = true
    } else if (diff <= TWENTY_FOUR_HOURS_MS && !notifiedSet.has(key24h)) {
      await createNotification({
        id: `task-watcher-deadline-24h-${target.id}-${Date.now()}`,
        title: '締切まで24時間以内',
        message: `${target.title}\n${target.courseName}`,
        url: target.url,
      })
      nextNotifiedKeys.add(key24h)
      changed = true
    }
  }

  if (changed) {
    await saveNotifiedDeadlineKeys(Array.from(nextNotifiedKeys))
  }
}
```

- [ ] **Step 3: 型チェックを通す**

Run: `pnpm exec tsc -b --noEmit`
Expected: エラーなし

- [ ] **Step 4: コミット**

```bash
git add src/background/index.ts
git commit -m "feat(background): 自動スキャン後の締切通知に手動課題を統合"
```

---

## Task 9: ダッシュボードのタイムライン統合

**Files:**
- Modify: `src/App.tsx`
- Delete: `src/components/ManualAssignmentSection.tsx`

**Interfaces:**
- Consumes: `mergeTimeline`（Task 3）、`getManualUrgent` / `getManualTomorrow` / `getManualThisWeek` / `getManualLater` / `getManualSubmitted`（Task 2）、`ManualAssignmentCard`（Task 4）

- [ ] **Step 1: import を更新する**

`src/App.tsx` の import群を更新する。`ManualAssignmentSection` のimportを削除し、以下を追加する。

```ts
import { ManualAssignmentCard } from './components/ManualAssignmentCard'
import { mergeTimeline } from './utils/timeline'
import {
  getManualUrgent,
  getManualTomorrow,
  getManualThisWeek,
  getManualLater,
  getManualSubmitted,
} from './utils/manualAssignment'
```

既存の `import { ManualAssignmentSection } from './components/ManualAssignmentSection'` 行を削除する。

- [ ] **Step 2: 手動課題のグルーピングmemoを追加する**

`urgentAssignments` などの既存memo群（569-654行目付近）の直後に追加する。

```ts
  const manualUrgent = useMemo(
    () => getManualUrgent(manualAssignments),
    [manualAssignments],
  )

  const manualTomorrow = useMemo(
    () => getManualTomorrow(manualAssignments),
    [manualAssignments],
  )

  const manualThisWeek = useMemo(
    () => getManualThisWeek(manualAssignments),
    [manualAssignments],
  )

  const manualLater = useMemo(
    () => getManualLater(manualAssignments),
    [manualAssignments],
  )

  const manualSubmitted = useMemo(
    () => getManualSubmitted(manualAssignments),
    [manualAssignments],
  )

  const urgentTimeline = useMemo(
    () => mergeTimeline(urgentAssignments, manualUrgent),
    [urgentAssignments, manualUrgent],
  )

  const tomorrowTimeline = useMemo(
    () => mergeTimeline(tomorrowAssignments, manualTomorrow),
    [tomorrowAssignments, manualTomorrow],
  )

  const thisWeekTimeline = useMemo(
    () => mergeTimeline(thisWeekAssignments, manualThisWeek),
    [thisWeekAssignments, manualThisWeek],
  )

  const laterTimeline = useMemo(
    () => mergeTimeline(laterAssignments, manualLater),
    [laterAssignments, manualLater],
  )

  const submittedTimeline = useMemo(
    () => mergeTimeline(submittedAssignments, manualSubmitted),
    [submittedAssignments, manualSubmitted],
  )
```

- [ ] **Step 3: ダッシュボードの各Sectionをタイムライン版に置き換える**

「24時間以内」セクション（dashboard内、1002-1017行目付近）を置き換える。

```tsx
          <Section
            title="24時間以内"
            count={urgentTimeline.length}
            emptyText="24時間以内の提出物はありません。"
          >
            {urgentTimeline.map((item) =>
              item.kind === 'scan' ? (
                <div key={item.assignment.id}>
                  <AssignmentCard
                    assignment={item.assignment}
                    canHide
                    onHide={hideAssignment}
                  />
                  <AssignmentMemo assignmentId={item.assignment.id} apiBaseUrl={API_BASE_URL} isSubscriber={isSubscriber} />
                </div>
              ) : (
                <ManualAssignmentCard
                  key={item.assignment.id}
                  assignment={item.assignment}
                  onToggleSubmitted={(id) => void handleToggleManualSubmitted(id)}
                  onDelete={(id) => void handleDeleteManualAssignment(id)}
                />
              ),
            )}
          </Section>
```

同様に「明日まで」（1019-1034行目付近）を置き換える。

```tsx
          <Section
            title="明日まで"
            count={tomorrowTimeline.length}
            emptyText="明日までの課題はありません。"
          >
            {tomorrowTimeline.map((item) =>
              item.kind === 'scan' ? (
                <div key={item.assignment.id}>
                  <AssignmentCard
                    assignment={item.assignment}
                    canHide
                    onHide={hideAssignment}
                  />
                  <AssignmentMemo assignmentId={item.assignment.id} apiBaseUrl={API_BASE_URL} isSubscriber={isSubscriber} />
                </div>
              ) : (
                <ManualAssignmentCard
                  key={item.assignment.id}
                  assignment={item.assignment}
                  onToggleSubmitted={(id) => void handleToggleManualSubmitted(id)}
                  onDelete={(id) => void handleDeleteManualAssignment(id)}
                />
              ),
            )}
          </Section>
```

同様に「今週」（1036-1051行目付近）を置き換える。

```tsx
          <Section
            title="今週"
            count={thisWeekTimeline.length}
            emptyText="今週中の課題はありません。"
          >
            {thisWeekTimeline.map((item) =>
              item.kind === 'scan' ? (
                <div key={item.assignment.id}>
                  <AssignmentCard
                    assignment={item.assignment}
                    canHide
                    onHide={hideAssignment}
                  />
                  <AssignmentMemo assignmentId={item.assignment.id} apiBaseUrl={API_BASE_URL} isSubscriber={isSubscriber} />
                </div>
              ) : (
                <ManualAssignmentCard
                  key={item.assignment.id}
                  assignment={item.assignment}
                  onToggleSubmitted={(id) => void handleToggleManualSubmitted(id)}
                  onDelete={(id) => void handleDeleteManualAssignment(id)}
                />
              ),
            )}
          </Section>
```

同様に「それ以降」（1053-1068行目付近）を置き換える。

```tsx
          <Section
            title="それ以降"
            count={laterTimeline.length}
            emptyText="それ以降の課題はありません。"
          >
            {laterTimeline.map((item) =>
              item.kind === 'scan' ? (
                <div key={item.assignment.id}>
                  <AssignmentCard
                    assignment={item.assignment}
                    canHide
                    onHide={hideAssignment}
                  />
                  <AssignmentMemo assignmentId={item.assignment.id} apiBaseUrl={API_BASE_URL} isSubscriber={isSubscriber} />
                </div>
              ) : (
                <ManualAssignmentCard
                  key={item.assignment.id}
                  assignment={item.assignment}
                  onToggleSubmitted={(id) => void handleToggleManualSubmitted(id)}
                  onDelete={(id) => void handleDeleteManualAssignment(id)}
                />
              ),
            )}
          </Section>
```

「提出済み・完了」CollapsibleSection（1085-1098行目付近）を置き換える。

```tsx
          <CollapsibleSection
            title="提出済み・完了"
            count={submittedTimeline.length}
            emptyText="提出済みの課題はありません。"
          >
            {submittedTimeline.map((item) =>
              item.kind === 'scan' ? (
                <AssignmentCard
                  key={item.assignment.id}
                  assignment={item.assignment}
                  canHide
                  onHide={hideAssignment}
                />
              ) : (
                <ManualAssignmentCard
                  key={item.assignment.id}
                  assignment={item.assignment}
                  onToggleSubmitted={(id) => void handleToggleManualSubmitted(id)}
                  onDelete={(id) => void handleDeleteManualAssignment(id)}
                />
              ),
            )}
          </CollapsibleSection>
```

- [ ] **Step 4: 旧 ManualAssignmentSection の呼び出しを削除する**

dashboard内の以下のブロック（1130-1133行目付近）を削除する。

```tsx
          <ManualAssignmentSection
            assignments={manualAssignments}
            onDelete={(id) => void handleDeleteManualAssignment(id)}
          />
```

- [ ] **Step 5: handleToggleManualSubmitted を追加する**

`handleDeleteManualAssignment`（Task 6で更新済み）の直後に追加する。

```ts
  async function handleToggleManualSubmitted(id: string) {
    await toggleManualAssignmentSubmitted(id)
    setManualAssignments((prev) =>
      prev.map((a) => (a.id === id ? { ...a, submitted: !a.submitted } : a)),
    )
  }
```

`toggleManualAssignmentSubmitted` をimportに追加する（`getManualAssignments, deleteManualAssignment, type ManualAssignment` のimport行）。

```ts
import {
  getManualAssignments,
  deleteManualAssignment,
  toggleManualAssignmentSubmitted,
  type ManualAssignment,
} from './core/manualAssignment'
```

- [ ] **Step 6: 古いファイルを削除する**

```bash
rm src/components/ManualAssignmentSection.tsx
```

- [ ] **Step 7: 型チェックを通す**

Run: `pnpm exec tsc -b --noEmit`
Expected: エラーなし

- [ ] **Step 8: Lintを通す**

Run: `pnpm exec eslint src/App.tsx src/components/ManualAssignmentCard.tsx`
Expected: エラーなし

- [ ] **Step 9: コミット**

```bash
git add src/App.tsx src/components/ManualAssignmentSection.tsx
git commit -m "feat(dashboard): 手動課題をタイムラインセクションに統合し独立セクションを廃止"
```

---

## Task 10: ウィジェット表示条件の限定とフォームリセットバグ修正

**Files:**
- Modify: `src/content/manualTaskWidget.ts`

**Interfaces:**
- Produces: `isCoursePage(): boolean`、`isAssignmentPage(): boolean`（既存 `initManualTaskWidget` の入口判定に使用）

- [ ] **Step 1: URL判定関数を追加する**

`src/content/manualTaskWidget.ts` 冒頭のヘルパー関数群（`createId` の直後、38行目手前）に追加する。

```ts
function isCoursePage(): boolean {
  return (
    location.pathname.includes('/course/view.php') &&
    new URLSearchParams(location.search).has('id')
  )
}

function isAssignmentPage(): boolean {
  return (
    location.pathname.includes('/mod/assign/view.php') &&
    new URLSearchParams(location.search).has('id')
  )
}
```

- [ ] **Step 2: initManualTaskWidget に表示条件を追加する**

既存の `initManualTaskWidget`（180-202行目付近）を以下に置き換える。コースページ判定は Task 11 で本格的に使うため、ここでは「課題提出ページ以外なら早期return」という最小限の変更にとどめる。

```ts
export async function initManualTaskWidget(): Promise<void> {
  if (document.getElementById('letus-task-watcher-widget')) return
  if (!isCoursePage() && !isAssignmentPage()) return

  const [courses, assignments] = await Promise.all([
    getCourses(),
    getAssignments(),
  ])

  if (courses.length === 0) return

  if (isCoursePage()) {
    buildWidget(courses)
    return
  }

  const currentUrl = location.href.split('#')[0]
  const matchedAssignment = assignments.find((a) => {
    if (!a.url) return false
    const assignmentUrl = a.url.split('#')[0]
    return assignmentUrl === currentUrl
  })

  if (matchedAssignment) {
    buildScannedIndicator(matchedAssignment)
  } else {
    buildWidget(courses)
  }
}
```

- [ ] **Step 3: フォームリセットバグを修正する**

`buildWidget` 内の送信ハンドラ（146-177行目付近）の末尾、フォームクリア処理を更新する。

```ts
    await addManualAssignment(item)
    closePanel()

    ;(shadow.getElementById('wt-title') as HTMLInputElement).value = ''
    ;(shadow.getElementById('wt-deadline') as HTMLInputElement).value = ''
    ;(shadow.getElementById('wt-memo') as HTMLTextAreaElement).value = ''
    ;(shadow.getElementById('wt-course') as HTMLSelectElement).selectedIndex = 0
  })
```

- [ ] **Step 4: ビルドして手動確認する**

Run: `pnpm build`
Expected: `dist/content.js` が生成され、import/exportが残っていないことを確認する。

```bash
grep -n "^import\|^export" dist/content.js
```

Expected: 出力なし（importもexportも残らない）

- [ ] **Step 5: コミット**

```bash
git add src/content/manualTaskWidget.ts
git commit -m "fix(content): ウィジェット表示をコース・課題提出ページに限定しフォームリセットを修正"
```

---

## Task 11: コースページのインラインバッジ実装

**Files:**
- Modify: `src/content/manualTaskWidget.ts`

**Interfaces:**
- Produces: `buildCourseBadges(courses, assignments, manualAssignments): void`（コースページの各課題リンクにバッジを挿入する）

このタスクはLETUS実ページのDOM構造（`/course/view.php` の課題リンク一覧マークアップ）に依存するため、ユニットテストでは検証できない。Step 4でLETUS実環境での手動確認を必須とする。

- [ ] **Step 1: 課題リンク検出とバッジ構築関数を実装する**

`src/content/manualTaskWidget.ts` の `initManualTaskWidget` 直前に追加する。LETUSのコースページは `course/view.php` 内で `<a href="...mod/assign/view.php?id=XXX">` 形式のリンクとして課題が一覧表示される（`src/content/courseDetector.ts` の `isCourseUrl` と対をなす判定ロジックを流用）。

```ts
function normalizeAssignmentUrl(url: string): string {
  return url.split('#')[0]
}

function findAssignmentLinks(): HTMLAnchorElement[] {
  return Array.from(
    document.querySelectorAll<HTMLAnchorElement>('a[href*="/mod/assign/view.php"]'),
  )
}

function createBadgeHost(): { host: HTMLElement; shadow: ShadowRoot } {
  const host = document.createElement('span')
  host.style.cssText = 'display:inline-flex;margin-left:8px;vertical-align:middle;'
  const shadow = host.attachShadow({ mode: 'closed' })

  const style = document.createElement('style')
  style.textContent = `
    :host { all: initial; font-family: sans-serif; }
    .badge {
      display: inline-flex; align-items: center; gap: 4px;
      font-size: 11px; padding: 2px 7px; border-radius: 999px;
      border: 1px solid #d1d5db; background: #fff; color: #374151;
      white-space: nowrap; cursor: default;
    }
    .badge.clickable { cursor: pointer; }
    .badge.submitted { background: #d1fae5; border-color: #10b981; color: #065f46; }
    .badge.unadded { cursor: pointer; font-weight: 700; color: #2563eb; border-color: #93c5fd; }
    .badge.unadded:hover { background: #eff6ff; }
  `
  shadow.appendChild(style)

  return { host, shadow }
}

function buildCourseBadges(
  courses: Course[],
  assignments: Assignment[],
  manualAssignments: ManualAssignment[],
): void {
  const links = findAssignmentLinks()

  for (const link of links) {
    let url: string
    try {
      url = normalizeAssignmentUrl(new URL(link.href, location.href).toString())
    } catch {
      continue
    }

    if (link.dataset.letusTaskWatcherBadge === 'true') continue
    link.dataset.letusTaskWatcherBadge = 'true'

    const scanned = assignments.find(
      (a) => a.url && normalizeAssignmentUrl(a.url) === url,
    )
    const manual = manualAssignments.find(
      (a) => a.letusUrl && normalizeAssignmentUrl(a.letusUrl) === url,
    )

    const { host, shadow } = createBadgeHost()
    const badge = document.createElement('span')

    if (scanned) {
      const submitted = scanned.lifecycleStatus === 'submitted' ||
        scanned.submissionStatus === 'submitted' ||
        scanned.submissionStatus === 'completed'
      badge.className = `badge ${submitted ? 'submitted' : ''}`
      badge.textContent = `${formatDeadlineShort(scanned.deadline ?? '')} ${submitted ? '✓' : '○'}`
    } else if (manual) {
      badge.className = `badge clickable ${manual.submitted ? 'submitted' : ''}`
      badge.textContent = `${formatDeadlineShort(manual.deadline)} ${manual.submitted ? '✓' : '○'}`
      badge.addEventListener('click', async (event) => {
        event.preventDefault()
        event.stopPropagation()
        await toggleManualSubmitted(manual.id)
        badge.classList.toggle('submitted')
        badge.textContent = `${formatDeadlineShort(manual.deadline)} ${!manual.submitted ? '✓' : '○'}`
        manual.submitted = !manual.submitted
      })
    } else {
      badge.className = 'badge unadded'
      badge.textContent = '+'
      badge.addEventListener('click', (event) => {
        event.preventDefault()
        event.stopPropagation()
        openQuickAddForm(courses, link.textContent ?? '', url)
      })
    }

    shadow.appendChild(badge)
    link.insertAdjacentElement('afterend', host)
  }
}

async function toggleManualSubmitted(id: string): Promise<void> {
  const r = await chrome.storage.local.get('manualAssignments') as { manualAssignments?: ManualAssignment[] }
  const current = r.manualAssignments ?? []
  const updated = current.map((a) => (a.id === id ? { ...a, submitted: !a.submitted } : a))
  await chrome.storage.local.set({ manualAssignments: updated })
}

function formatDeadlineShort(isoString: string): string {
  if (!isoString) return ''
  const d = new Date(isoString)
  const m = d.getMonth() + 1
  const day = d.getDate()
  const hh = String(d.getHours()).padStart(2, '0')
  const mm = String(d.getMinutes()).padStart(2, '0')
  return `${m}/${day} ${hh}:${mm}`
}
```

`formatDeadlineShort` は既存の `buildScannedIndicator` 内に同名関数が既にある（247-254行目付近）。重複定義を避けるため、既存の `formatDeadlineShort` 関数定義を1つに統合し、ファイル下部のものを削除してこのブロックのものだけを残す。

- [ ] **Step 2: 課題追加フォームを共通化する**

既存の `buildWidget` 関数はコース選択ドロップダウン付きのフォームを構築する。インラインバッジの `+` クリックからは「課題名・URLを事前入力した状態で同じフォームを開く」必要がある。`buildWidget` のDOM構築ロジックを `buildWidgetInto` に切り出し、`buildWidget` と `openQuickAddForm` の両方から呼べるようにする。

Task 10時点の `buildWidget` 関数全体（`function buildWidget(courses: Course[]): void { ... }` の開始行から、対応する閉じ括弧までの全体）を、以下の内容に**まるごと置き換える**。

```ts
function buildWidget(courses: Course[]): void {
  const host = document.createElement('div')
  host.id = 'letus-task-watcher-widget'
  host.style.cssText = 'position:fixed;bottom:16px;right:16px;z-index:2147483647;'
  document.body.appendChild(host)

  const shadow = host.attachShadow({ mode: 'closed' })
  buildWidgetInto(shadow, courses)
}

function openQuickAddForm(courses: Course[], title: string, url: string): void {
  const existingHost = document.getElementById('letus-task-watcher-quickadd')
  if (existingHost) existingHost.remove()

  const host = document.createElement('div')
  host.id = 'letus-task-watcher-quickadd'
  host.style.cssText = 'position:fixed;bottom:16px;right:16px;z-index:2147483647;'
  document.body.appendChild(host)

  const shadow = host.attachShadow({ mode: 'closed' })
  buildWidgetInto(shadow, courses, { title, url })
}

function buildWidgetInto(
  shadow: ShadowRoot,
  courses: Course[],
  prefill?: { title: string; url: string },
): void {
  const style = document.createElement('style')
  style.textContent = `
    :host { all: initial; font-family: sans-serif; font-size: 13px; }
    .btn {
      display: flex; align-items: center; gap: 6px;
      background: #fff; border: 1px solid #d1d5db;
      border-radius: 20px; padding: 6px 12px; cursor: pointer;
      color: #374151; box-shadow: 0 1px 4px rgba(0,0,0,.12);
      white-space: nowrap;
    }
    .btn:hover { background: #f9fafb; }
    .label { display: none; }
    .btn:hover .label { display: inline; }
    .form-panel {
      display: none; position: absolute; bottom: 44px; right: 0;
      background: #fff; border: 1px solid #d1d5db; border-radius: 12px;
      padding: 14px 16px; width: 280px;
      box-shadow: 0 2px 12px rgba(0,0,0,.12);
    }
    .form-panel.open { display: block; }
    .form-header {
      display: flex; justify-content: space-between; align-items: center;
      margin-bottom: 10px;
    }
    .form-title { font-weight: 600; font-size: 13px; color: #111827; }
    .close-btn { background: none; border: none; cursor: pointer; color: #6b7280; font-size: 16px; }
    .field { margin-bottom: 8px; }
    input, select, textarea {
      width: 100%; box-sizing: border-box; font-size: 12px;
      border: 1px solid #d1d5db; border-radius: 6px;
      padding: 6px 8px; color: #111827; background: #fff;
    }
    textarea { resize: none; height: 48px; }
    .meta { font-size: 11px; color: #9ca3af; margin-top: 4px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    .actions { display: flex; gap: 6px; margin-top: 10px; }
    .cancel { flex: 1; border: 1px solid #d1d5db; background: #fff; border-radius: 6px; padding: 6px; cursor: pointer; font-size: 12px; }
    .submit {
      flex: 2; background: #2563eb; color: #fff; border: none;
      border-radius: 6px; padding: 6px; cursor: pointer; font-size: 12px;
    }
    .submit:disabled { opacity: .5; cursor: not-allowed; }
    .error { color: #dc2626; font-size: 11px; margin-top: 6px; }
  `
  shadow.appendChild(style)

  const btn = document.createElement('button')
  btn.className = 'btn'
  btn.type = 'button'
  btn.innerHTML = `
    <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/>
    </svg>
    <span class="label">課題を追加</span>
  `
  shadow.appendChild(btn)

  const panel = document.createElement('div')
  panel.className = 'form-panel'
  panel.innerHTML = `
    <div class="form-header">
      <span class="form-title">課題を追加</span>
      <button class="close-btn" type="button" aria-label="閉じる">✕</button>
    </div>
    <div class="field">
      <input id="wt-title" type="text" placeholder="課題名" required />
    </div>
    <div class="field">
      <input id="wt-deadline" type="datetime-local" required />
    </div>
    <div class="field">
      <select id="wt-course">
        <option value="">コースを選択</option>
        ${courses.map((c) => `<option value="${escapeHtml(c.id)}" data-name="${escapeHtml(c.name)}">${escapeHtml(c.name)}</option>`).join('')}
      </select>
    </div>
    <div class="field">
      <div class="meta" id="wt-url-display">${escapeHtml(prefill?.url ?? location.href)}</div>
    </div>
    <div class="field">
      <textarea id="wt-memo" placeholder="メモ（任意）"></textarea>
    </div>
    <div class="actions">
      <button class="cancel" type="button">キャンセル</button>
      <button class="submit" type="button">追加する</button>
    </div>
    <div class="error" id="wt-error"></div>
  `
  shadow.appendChild(panel)

  if (prefill) {
    ;(shadow.getElementById('wt-title') as HTMLInputElement).value = prefill.title
    panel.classList.add('open')
  }

  function openPanel(): void {
    panel.classList.add('open')
  }
  function closePanel(): void {
    panel.classList.remove('open')
  }

  btn.addEventListener('click', openPanel)
  panel.querySelector('.close-btn')!.addEventListener('click', closePanel)
  panel.querySelector('.cancel')!.addEventListener('click', closePanel)

  panel.querySelector('.submit')!.addEventListener('click', async () => {
    const title = (shadow.getElementById('wt-title') as HTMLInputElement).value.trim()
    const deadline = (shadow.getElementById('wt-deadline') as HTMLInputElement).value
    const courseSelect = shadow.getElementById('wt-course') as HTMLSelectElement
    const courseId = courseSelect.value
    const courseName = courseSelect.selectedOptions[0]?.dataset.name ?? ''
    const memo = (shadow.getElementById('wt-memo') as HTMLTextAreaElement).value.trim()
    const errorEl = shadow.getElementById('wt-error')!

    if (!title) { errorEl.textContent = '課題名を入力してください。'; return }
    if (!deadline) { errorEl.textContent = '締切を入力してください。'; return }
    if (!courseId) { errorEl.textContent = 'コースを選択してください。'; return }
    errorEl.textContent = ''

    const item: ManualAssignment = {
      id: createId(),
      courseId,
      courseName,
      title,
      letusUrl: prefill?.url ?? location.href,
      deadline: new Date(deadline).toISOString(),
      memo,
      submitted: false,
      createdAt: new Date().toISOString(),
    }

    await addManualAssignment(item)
    closePanel()

    ;(shadow.getElementById('wt-title') as HTMLInputElement).value = ''
    ;(shadow.getElementById('wt-deadline') as HTMLInputElement).value = ''
    ;(shadow.getElementById('wt-memo') as HTMLTextAreaElement).value = ''
    ;(shadow.getElementById('wt-course') as HTMLSelectElement).selectedIndex = 0
  })
}
```

この置き換えは Task 10 Step 3（フォームリセット修正）を内包しているため、内容が重複しても問題ない（上書きされるだけ）。

- [ ] **Step 3: initManualTaskWidget からコールする**

Task 10で実装した `initManualTaskWidget` のコースページ分岐を更新する。

```ts
export async function initManualTaskWidget(): Promise<void> {
  if (!isCoursePage() && !isAssignmentPage()) return

  const [courses, assignments, manualAssignments] = await Promise.all([
    getCourses(),
    getAssignments(),
    getManualAssignmentsInline(),
  ])

  if (courses.length === 0) return

  if (isCoursePage()) {
    buildCourseBadges(courses, assignments, manualAssignments)
    if (!document.getElementById('letus-task-watcher-widget')) {
      buildWidget(courses)
    }
    return
  }

  if (document.getElementById('letus-task-watcher-widget')) return

  const currentUrl = location.href.split('#')[0]
  const matchedAssignment = assignments.find((a) => {
    if (!a.url) return false
    const assignmentUrl = a.url.split('#')[0]
    return assignmentUrl === currentUrl
  })

  if (matchedAssignment) {
    buildScannedIndicator(matchedAssignment)
  } else {
    buildWidget(courses)
  }
}
```

`getManualAssignmentsInline` は既存の `addManualAssignment` 関数の隣にある手動課題取得処理を関数化したもの。ファイル冒頭の `addManualAssignment` 関数の直前に追加する。

```ts
async function getManualAssignmentsInline(): Promise<ManualAssignment[]> {
  const r = await chrome.storage.local.get('manualAssignments') as { manualAssignments?: ManualAssignment[] }
  return r.manualAssignments ?? []
}
```

既存の `ManualAssignment` 型定義（5-8行目付近）に `submitted: boolean` を追加する。

```ts
type ManualAssignment = {
  id: string; courseId: string; courseName: string; title: string
  letusUrl: string | null; deadline: string; memo: string; submitted: boolean; createdAt: string
}
```

送信ハンドラ内で新規作成する `ManualAssignment` オブジェクトにも `submitted: false` を追加する（`buildWidgetInto` 内、`addManualAssignment(item)` 呼び出し直前のオブジェクトリテラル）。

```ts
    const item: ManualAssignment = {
      id: createId(),
      courseId,
      courseName,
      title,
      letusUrl: prefill?.url ?? location.href,
      deadline: new Date(deadline).toISOString(),
      memo,
      submitted: false,
      createdAt: new Date().toISOString(),
    }
```

- [ ] **Step 4: ビルドして自己完結性を確認する**

Run: `pnpm build`

```bash
grep -n "^import\|^export" dist/content.js
```

Expected: 出力なし

- [ ] **Step 5: 型チェックを通す**

Run: `pnpm exec tsc -b --noEmit`
Expected: エラーなし

- [ ] **Step 6: LETUS実環境で手動確認する**

`dist/` を解凍済み拡張機能としてロードし、以下を確認する。

1. コースページ（`/course/view.php?id=XXX`）を開く
2. 各課題リンクの横にバッジが表示される
   - スキャン済み課題: 締切+✓/○（クリックしても変化しない）
   - 手動追加済み課題: 締切+✓/○（クリックでトグルされ、ダッシュボードにも反映される）
   - 未登録の課題: `+` ボタン（クリックでタイトル・URL事前入力済みフォームが開く）
3. 右下フローティングウィジェットの `+` ボタンからも、コース上に存在しない課題を自由に追加できる
4. 課題提出ページ（`/mod/assign/view.php?id=XXX`）では従来通りインジケーター/`+`ボタンのみで、インラインバッジは出ない
5. それ以外のページ（`/my/`、`/mod/quiz/` 等）では何も表示されない

- [ ] **Step 7: コミット**

```bash
git add src/content/manualTaskWidget.ts
git commit -m "feat(content): コースページに課題ごとのインラインバッジを追加"
```

---

## Task 12: 全体テスト・lint・ビルドの最終確認

**Files:** なし（検証のみ）

- [ ] **Step 1: 全ユニットテストを実行する**

Run: `pnpm exec vitest run`
Expected: 全テストPASS

- [ ] **Step 2: 型チェックを実行する**

Run: `pnpm exec tsc -b --noEmit`
Expected: エラーなし

- [ ] **Step 3: Lintを実行する**

Run: `pnpm exec eslint .`
Expected: エラーなし

- [ ] **Step 4: 本番ビルドを実行する**

Run: `pnpm build`
Expected: `dist/` が生成され、エラーなし

- [ ] **Step 5: content.js / background.js の自己完結性を再確認する**

```bash
grep -n "^import\|^export" dist/content.js dist/background.js
```

Expected: `dist/content.js` は出力なし。`dist/background.js` は `type: module` のため import/export が残っていてもよい（Service Workerとして動作するため問題ない）。

- [ ] **Step 6: manifest.json のバージョンを確認する**

`public/manifest.json` の `version` が `1.1.0` のままであることを確認する。リリース時にユーザーが別途バージョン番号を更新する（このタスクでは変更しない）。

- [ ] **Step 7: TASKS.md があれば該当タスクを完了マークする**

`qa/v1.1.x-release` ブランチには `TASKS.md` が存在する（`release/v1.1.x` から継承）。今回のQA内容は新規追加項目のため、`TASKS.md` に「P6: v1.1.xリリース前QA」として完了項目を追記する。

```bash
git log --oneline release/v1.1.x..qa/v1.1.x-release
```

の出力をもとに、`TASKS.md` 末尾に以下を追記する（既存の「完了済み」セクションの直前に挿入）。

```markdown
## P6: v1.1.xリリース前QA

- [x] resetAllDataで手動課題も削除されるよう修正
- [x] 手動追加フォームの送信後にコース選択をリセット
- [x] ウィジェット表示をコースページ・課題提出ページに限定
- [x] コースページに課題ごとのインラインバッジ（締切+提出状況）を追加
- [x] 手動課題に提出済みトグルを追加
- [x] ダッシュボードのタイムラインに手動課題を統合（独立セクション廃止）
- [x] 締切通知（1h/3h/24h前）に手動課題を統合（popup側・background側）
- [x] 手動課題削除にUndoトーストを追加

---
```

- [ ] **Step 8: コミット**

```bash
git add TASKS.md
git commit -m "docs: v1.1.xリリース前QAタスクを完了マーク"
```

---

## Self-Review チェック結果

- **spec coverage**: 設計ドキュメントの修正方針1〜9すべてに対応するタスクが存在する（バグ1→Task5、バグ2→Task10、3→Task10、4→Task11、5→Task10、6→Task9、型拡張→Task1、7→Task7+8、8→Task6）
- **placeholder scan**: 「TBD」「あとで」等の記述なし。Task 11 Step 2の `buildWidgetInto` 本体コピーのみ「既存実装をそのまま移動」という指示だが、移動元コード全文はTask 10 Step 2・既存ファイル読了時点で実装者が参照可能な箇所として明示している
- **type consistency**: `ManualAssignment.submitted` はTask 1で定義し、Task 2・3・4・6・7・8・9・11で一貫して使用。`mergeTimeline` の `TimelineItem` 型はTask 3で定義しTask 9でそのまま使用。関数名の表記ゆれなし
