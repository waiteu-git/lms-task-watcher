# Chrome Web Store 申請チェックリスト — v1.4.1

アップロード用パッケージ: **`letus-task-watcher-1.4.1.zip`**（`dist/` を zip 化・forward-slashパス・24エントリ・約628KB）
manifest version: **1.4.1**（**公開中の 1.4.0 からの通常アップデート**。Chrome/Edgeとも 2026-08-01 までに 1.4.0 が両ストアで公開済み）／ manifest v3 ／ 決済・外部サーバー通信なし（ローカル完結）

## このリリースの中身（v1.4.0 → v1.4.1 の差分）

**保守モードの即時レーン（実害バグ・9/11後期開始に間に合わせる期限あり）。新しい権限の追加はなし・新規ホストなし・外部送信ゼロ不変。**

1. **後期の時間割未取込を検出・案内**
   - 症状: 前期の時間割を取り込んだ利用者は、後期になっても検出されず、案内も出ていなかった（`resolveSemester`が取得済みのうち`capturedAt`最新のものを無条件に返し続けるため）
   - 修正: 日付から「あるべき学期」を判定する`calendarSemester`（2026年度後期開始日=9/11を確定値で保持）と、取得済みと突き合わせる`findMissingCurrentSemester`を追加。ダッシュボード（`TimetableSection`）とポップアップ（`TodayTimetable`）に案内カードを表示し、CLASSを開くボタンで誘導（CLASSのフォームを裏で操作することはしない設計）
2. **学期ごとの時間割取込通知**
   - 症状: 「時間割を取り込みました」のOS通知は一度発火すると二度と鳴らない仕様だった（前期分の通知後、後期分は永久に無音）
   - 修正: 通知済みフラグを学期ごとの複合キー配列へ移行（`notifiedDeadlineKeys`と同型）。前期通知済みでも後期は別途通知される
   - あわせて、拡張機能の更新のたびに通知済みフラグが再計算され後期分の履歴を巻き戻しうる潜在バグを修正（`handleInstalled`の移行バックフィルを冪等化）
3. **Edge通過に伴うLP表記の整理（`landing/index.html`のみ・拡張本体は無変更）**
   - ヒーローピル・デモ節本文・FAQ（JSON-LD/表示の両方）から「Edge版は順次対応/旧バージョン」の但し書きを撤去（両ストアの版が揃ったため）

## 0. 申請前チェック（機械的検証・2026-09-01）

- [x] `pnpm exec vitest run src` → **50 files / 725 passed / 0 FAIL**（v1.4.0 の 706 → +19）
- [x] `pnpm exec tsc -b` → エラー0
- [x] `pnpm run lint` → エラー0（既存 exhaustive-deps warning 4件のみ・不変）
- [x] `pnpm run build` → 成功
- [x] `dist/content.js` / `dist/classTimetable.js` に `import` 文なし（grep 0件）
- [x] `dist/manifest.json` の `version` = **1.4.1**、`host_permissions` は `letus.ed.tus.ac.jp` / `class.admin.tus.ac.jp` の2つのみ（**新規ホスト・新規権限なし**）
- [x] zip 内パスに backslash なし（`unzip -l` grep 0件）
- [x] develop へ push 済み（`11a2c1f`=拡張本体の修正・`1a3eacd`=LP表記整理）
- [ ] 実機確認（未・ユーザー操作待ち）: 後期のCLASS時間割を開いていない状態でダッシュボード/ポップアップを開き、案内カードが表示されること／CLASSを開くボタンでCLASSトップへ遷移すること／CLASSで後期を表示後に再度開くと取り込まれ通知が届き案内カードが消えること

## 1. ストア掲載文の更新（提出時）

- 「新機能」欄: 後期の時間割未取込を検出してお知らせする機能を追加（バグ修正）
- データセーフティ/権限の申告変更: **なし**（収集項目・送信先・権限とも v1.4.0 から不変）

## 2. 申請方針

- **保守モード方針により Chrome/Edge 同日申請**（版差を審査期間差だけに縮める）
- 9/11（後期開始）までに両ストアで承認・配信される必要あり。Edge審査は数日〜2週間の実績があるため、**早めの申請を推奨**
